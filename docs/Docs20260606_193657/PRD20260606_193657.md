# PRD - Fonte Nomus (ERP) / Ingestao de Processos

> Projeto: DLH Core (substrato de memoria operacional + cockpit de ingestao).
> Feature: adicionar o ERP Nomus como nova fonte de dados, comecando pela ingestao dos Processos do tipo "Venda Governamental".
> Base: discovery20260606_193657.md, stories-requisitos20260606_193657.md (validados) e analise do repositorio dlh-core (CLAUDE.md).
> Data: 2026-06-06.

---

## 1. Resumo Executivo

O DLH Core e o substrato de memoria operacional do ecossistema DLH, alimentado por um cockpit de ingestao de uso interno. Hoje a Fase 1 opera com um unico conector ativo (portal Effecti), que coleta avisos/editais de licitacao, preserva o conteudo verbatim, indexa para busca semantica e persiste tudo integralmente para consumo da IA Lia via API/MCP. Esta feature adiciona o ERP Nomus como segunda fonte de dados do substrato, comecando pela ingestao dos Processos do tipo "Venda Governamental" das empresas famaha e darlu.

O problema que a feature resolve e a ausencia de memoria operacional interna no cerebro do DLH. Enquanto o Effecti traz editais externos (oportunidades de fora), o Nomus traz dados internos do ERP (vendas governamentais em andamento). Um processo Nomus do tipo "venda governamental" e a continuacao do ciclo de vida de um aviso aprovado: o aviso vem do Effecti, passa por aprovacao humana, e e cadastrado no Nomus como venda governamental. Ingerir esses processos enriquece o substrato com o estado operacional real das licitacoes em que a DLH participa, habilitando a Lia a recuperar memoria relevante por similaridade textual.

A integracao se da reaproveitando o PADRAO arquitetural ja existente (paginacao, backoff de throttling, sync incremental, deduplicacao, indexacao por embeddings, observabilidade), mas com contrato de dados e persistencia proprios do dominio de processos. O Effecti permanece intacto como fonte de editais externos; o Nomus entra como fonte multi-recurso de dados internos. O escopo desta entrega cobre apenas o recurso `/processos` filtrando `tipo = "Venda Governamental"`, ja desenhado para acomodar recursos e tipos futuros (cobranca, propostas, pedidos, NFes, contas a receber) por simples toggle, sem reescrita.

---

## 2. Contexto do Projeto

### Stack atual
- **Linguagem:** TypeScript ponta a ponta.
- **Frontend:** Next.js 15.5 (App Router) + React 19, Tailwind CSS 3.4 + shadcn/ui, Lucide, TanStack Query 5, react-hook-form + zod, `@supabase/ssr`. Deploy alvo Vercel.
- **Backend:** Supabase Edge Functions (Deno/TypeScript) em `supabase/functions`.
- **Substrato:** Supabase / PostgreSQL + pgvector + Auth + Storage + Edge Functions + pg_cron + Realtime + Vault.
- **Autenticacao:** Supabase Auth, login exclusivamente via Google (OAuth). Perfil unico no MVP ("interno").
- **Embeddings:** provider plugavel, padrao bge-m3 self-hosted, `vector(1024)`, indice HNSW (`vector_cosine_ops`).

### Arquitetura relevante
- O projeto ja foi desenhado para multiplos conectores. A interface `SourceConnector` (em `supabase/functions/_shared/effecti-connector.ts`) e desacoplada de `fontes.tipo`, e a factory `createConnector(tipo, config)` aceita novos tipos sem refatorar o Effecti.
- A tabela `fontes` tem campo `tipo` extensivel a novos conectores; credenciais ficam no Supabase Vault por `fonte.id`. `getFonteByTipo()` (em `_shared/vault.ts`) ja aceita parametro de tipo, com default `effecti`.
- O substrato central `avisos` e moldado para editais do Effecti (`effecti_id UNIQUE NOT NULL` como chave de dedup); `runIncrementalSync`/`upsertBatch` fazem upsert em `avisos` com `onConflict: effecti_id`.
- A busca semantica usa `aviso_chunks` (HNSW, `vector(1024)`) e a RPC `busca_semantica_chunks`, exposta pelo endpoint `/v1` `v1-substrato-busca-semantica`.
- A observabilidade usa as tabelas `execucoes` (Realtime) e `erros_ingestao`. O agendamento usa pg_cron e `config_agendamento` com orquestrador global anti-sobreposicao.
- O Design Lock (`manifest.json locked:true`, navegacao em `src/lib/nav.ts`) restringe novas telas/itens de menu.

### Modulos afetados
- `_shared/effecti-connector.ts` (interface `SourceConnector`, factory `createConnector`, `fetchWithBackoff`/`parseRetryAfter`/`computeBackoff`).
- `_shared/vault.ts` (`getFonteByTipo`, `setFonteSecret`).
- `_shared/embeddings.ts` (`chunkText`, `generateAndStoreChunks`, provider plugavel).
- `pipeline.ts` (persistencia, controle `status_indexacao`, isolamento de falha por item).
- Endpoints `fontes-credencial`, `fontes-testar`, `ingestao-config`, `ingestao-coletar`, `ingestao-orquestrar`, `v1-substrato-busca-semantica`.
- Tabelas `fontes`, `config_ingestao`, `execucoes`, `erros_ingestao`, `aviso_chunks`; nova tabela `nomus_processos`.
- UI: `fontes/page.tsx`, `fonte-effecti-block.tsx`, `cred-form.tsx`, `cfg-form.tsx`, `execucoes-client.tsx`, `erros-client.tsx`, `runs-table.tsx`, `erros-table.tsx`.

### API do Nomus (confirmado via doc Postman publica)
- Autenticacao: header `Authorization: Basic <chave-integracao-rest>` + `Content-Type: application/json`. A chave fica no Vault como segredo da fonte Nomus.
- Paginacao: parametro `?pagina=N` (incremental, default 1). `GET /rest/processos?pagina=N` retorna um ARRAY de processos; `GET /rest/processos/{id}` retorna o detalhe.
- Filtro por data (janela): `?query=campoData>yyyy-mm-ddTHH:mm:ss`. Filtro por campo: `?query=campo=valor;campo2=valor2`.
- Throttling: HTTP 429 com `tempoAteLiberar` (segundos a esperar). Observado na pratica: lotes (~14 chamadas) com pausa (~5s) entre lotes.
- Cada processo traz, entre outros: `id`, `tipo` (ex.: "Venda Governamental"), `pessoa`, `etapa`, `nome`, `reportador`, `responsavel`, `descricao`, `empresa`. O filtro por "Venda Governamental" e feito pelo campo `tipo` no cliente (a base tem ~720+ paginas).
- Multi-empresa: UMA unica instancia/base e UMA unica chave de integracao. As empresas (famaha, darlu) se distinguem pelo campo `empresa` em cada processo, nao por bases separadas.
- ACHADO CRITICO: os campos personalizados (portal_de_compras, orgao_gerenciador, numero-edital, uasg, prazos, etc.) sao WRITE-ONLY na API: podem ser gravados via `PUT` mas NAO sao retornados em nenhum GET. Logo, ficam indisponiveis por leitura e fora do escopo de ingestao.

---

## 3. Personas

### Persona 1: Operador do cockpit
- **Descricao:** usuario interno autorizado do nucleo DLH (perfil "interno", login via Google), responsavel por configurar fontes, disparar coletas e monitorar a saude da ingestao no cockpit.
- **Objetivos:**
  - Cadastrar e testar a credencial da fonte Nomus sem editar codigo nem expor segredos.
  - Selecionar quais recursos e tipos do Nomus sincronizar.
  - Disparar a coleta sob demanda e acompanhar o progresso ao vivo.
  - Monitorar execucoes, erros e saude da fonte com a mesma paridade do Effecti.
- **Frustacoes:**
  - Depender de mudanca de codigo/redeploy para ativar uma nova fonte ou recurso.
  - Falta de visibilidade sobre o progresso e os erros de uma coleta longa.
  - Mensagens de erro genericas que nao distinguem credencial invalida de rate limit ou timeout.

### Persona 2: Lia (IA consumidora do substrato)
- **Descricao:** IA do ecossistema DLH, consumidora read-only do substrato via API `/v1`, que recupera memoria operacional por significado.
- **Objetivos:**
  - Encontrar processos por similaridade textual (memoria de vendas governamentais).
  - Buscar memoria de forma federada entre origens, com cada resultado marcado por sua origem.
  - Filtrar opcionalmente o escopo da busca (tudo / processos / avisos / por tipo).
- **Frustacoes:**
  - Indice fragmentado por origem que exigisse novo endpoint a cada recurso.
  - Quebra de contrato de resposta que invalidasse seu consumo atual.
  - Resultados de uma origem poluindo consultas de outra origem.

---

## 4. User Stories

> Stories aprovadas, mantidas intactas do documento de stories e requisitos.

### Dominio A: Conexao e credencial da fonte Nomus

#### US-00 - Provisionar a fonte Nomus no substrato
**Como** desenvolvedor do DLH Core, **quero** que exista um registro da fonte `nomus` em `fontes` e sua `config_ingestao`, **para** que `getFonteByTipo("nomus")` resolva a fonte e os endpoints/coleta funcionem.
Referencia de codigo: seed `20260606120700_seed.sql` (hoje so cria a fonte Effecti); `getFonteByTipo()` lanca 404 quando a linha nao existe.
Criterios de aceite:
- Migracao/seed idempotente cria uma linha em `fontes` com `tipo = 'nomus'`, `endpoint_base` da instancia Nomus, `estado_conexao = 'nao_configurada'`, `token_cifrado = null`.
- A fonte Nomus recebe `ativa` e `ordem` para participar do ciclo do orquestrador.
- E criada a `config_ingestao` vinculada a fonte Nomus, com janela default de 7 dias e suporte a janela a partir de uma DATA ESPECIFICA (data inicial configuravel), nao apenas janela movel relativa.
- `getFonteByTipo("nomus")` passa a resolver a fonte sem 404.

#### US-01 - Cadastrar a credencial da fonte Nomus
**Como** operador do cockpit, **quero** cadastrar a chave de integracao REST do Nomus pela tela de Fontes, **para** habilitar a coleta sem editar codigo nem expor o segredo.
Referencia de codigo: padrao de `fontes-credencial/index.ts` (PUT, zod, `setFonteSecret`) e `_shared/vault.ts` (`setFonteSecret`, `getFonteByTipo`).
Criterios de aceite:
- A tela de Fontes exibe um bloco da fonte Nomus com campo para a chave de integracao.
- Ao salvar, a chave e gravada no Supabase Vault via RPC `set_fonte_secret` e `fontes.token_cifrado` guarda apenas a referencia.
- A chave nunca retorna ao cliente em nenhuma resposta nem aparece em log/auditoria.
- O endpoint exige sessao autorizada (`requireAuthorizedUser`) antes de processar o corpo.
- A acao e registrada em `audit_log` sem o valor do segredo (apenas referencia/fonte).
- Token vazio e rejeitado com erro de validacao (zod) e nada e gravado.

#### US-02 - Testar a conexao com o Nomus
**Como** operador do cockpit, **quero** testar a conexao com o Nomus, **para** confirmar que a credencial e o endpoint estao corretos antes de coletar.
Referencia de codigo: padrao de `fontes-testar/index.ts` e `EffectiConnector.testConnection` + `ConnectionTestError` em `_shared/effecti-connector.ts`.
Criterios de aceite:
- O teste faz uma requisicao leve ao Nomus (ex.: primeira pagina de `/rest/processos`) usando a chave do Vault lida em runtime.
- O resultado atualiza `fontes.estado_conexao` para `conectada`, `erro` ou `nao_configurada`.
- Falha 401 retorna mensagem de credencial invalida; 429 retorna mensagem de rate limit; timeout retorna mensagem de tempo excedido (causas distintas).
- A resposta inclui a latencia medida em milissegundos.
- Credencial ausente retorna estado `nao_configurada` com orientacao para cadastrar a chave antes de testar.
- A acao e auditada sem expor o segredo.

#### US-03 - Resolver a fonte por tipo parametrizado
**Como** desenvolvedor do DLH Core, **quero** que os endpoints de fonte resolvam a fonte pelo tipo informado (`effecti` ou `nomus`) em vez de fixar `effecti`, **para** que um segundo conector funcione sem duplicar endpoints.
Referencia de codigo: `getFonteByTipo()` em `_shared/vault.ts` (ja aceita parametro `tipo`, default `effecti`); paths fixos `/fontes/effecti/...` em `fontes-credencial` e `fontes-testar`.
Criterios de aceite:
- Os endpoints de credencial e teste aceitam o tipo da fonte como parametro (path ou corpo), nao mais fixo em `effecti`.
- `getFonteByTipo("nomus")` resolve a fonte Nomus a partir de `fontes.tipo = 'nomus'`.
- Chamar um tipo de fonte inexistente retorna 404 com mensagem clara.
- O comportamento existente do Effecti permanece inalterado (regressao zero nos fluxos atuais).

### Dominio B: Configuracao de recursos e tipos ativos

#### US-04 - Selecionar quais recursos do Nomus sincronizar
**Como** operador do cockpit, **quero** ligar/desligar quais recursos do Nomus sao ingeridos (processos agora; cobranca, propostas, pedidos, NFes, contas a receber no futuro), **para** controlar o escopo da coleta sem alterar codigo.
Referencia de codigo: modelo `config_ingestao` (analogo a `modalidades`/`portais`), endpoint `ingestao-config/index.ts`.
Criterios de aceite:
- O modelo persiste os recursos ativos numa coluna `jsonb` propria de `config_ingestao` (ex.: `recursos`), pois `modalidades`/`portais` (`text[] NOT NULL`) sao especificos do Effecti e nao comportam a estrutura recurso->tipos.
- A configuracao da fonte Nomus persiste a lista de recursos ativos.
- Nesta entrega, apenas o recurso `processos` vem ativo; os demais aparecem visiveis e desligados.
- A selecao persistida governa tanto a coleta manual quanto a agendada.
- Recursos desligados nao geram chamadas a API do Nomus.
- Alteracoes valem na proxima execucao, sem redeploy.

#### US-05 - Selecionar quais tipos por recurso sao ingeridos
**Como** operador do cockpit, **quero** escolher quais `tipo` dentro de `/processos` sao ingeridos (agora: "Venda Governamental"), **para** filtrar exatamente o subconjunto relevante sem trazer toda a base.
Referencia de codigo: filtro de allowlist no cliente do `EffectiConnector.collect` (Set de `modalidades`/`portais`).
Criterios de aceite:
- Os tipos ativos sao persistidos por recurso dentro da mesma estrutura `jsonb` (mapa recurso->tipos[]), nao em um `text[]` plano.
- A configuracao persiste, por recurso, a lista de tipos ativos.
- Nesta entrega, apenas `tipo = "Venda Governamental"` vem ativo para o recurso processos.
- O filtro por `tipo` e aplicado no cliente, sobre o array retornado pela API (a API nao filtra `tipo` por padrao confiavel para VG).
- Processos com `tipo` fora da allowlist sao descartados antes da persistencia.
- Lista de tipos vazia para um recurso ativo nao ingere nada daquele recurso (sem allowlist implicita de "todos").

### Dominio C: Conector e coleta da API Nomus

#### US-06 - Coletar processos paginados da API Nomus
**Como** operador do cockpit, **quero** que o conector Nomus percorra todas as paginas de `/rest/processos`, **para** que nenhum processo da janela seja perdido.
Referencia de codigo: padrao de paginacao do `EffectiConnector.collect` (loop de paginas ate esgotar); contrato `SourceConnector` e factory `createConnector` em `_shared/effecti-connector.ts`.
Criterios de aceite:
- O conector autentica com header `Authorization: Basic <chave>` e `Content-Type: application/json`.
- A paginacao usa o parametro `?pagina=N` incremental, comecando em 1.
- A listagem `GET /rest/processos?pagina=N` e tratada como array de processos; pagina vazia encerra a varredura.
- `createConnector("nomus", config)` retorna o conector Nomus sem alterar o caso `effecti`.
- Cada processo coletado preserva `id`, `tipo`, `pessoa`, `etapa`, `nome`, `reportador`, `responsavel`, `descricao`, `empresa` e o payload bruto integral.

#### US-07 - Respeitar o throttling do Nomus
**Como** desenvolvedor do DLH Core, **quero** que o conector respeite o rate limit do Nomus, **para** evitar bloqueios e perdas de coleta.
Referencia de codigo: `fetchWithBackoff` / `parseRetryAfter` / `computeBackoff` do `EffectiConnector`.
Criterios de aceite:
- Em HTTP 429, o conector aguarda o tempo indicado em `tempoAteLiberar` (segundos) antes de re-tentar.
- O conector processa em lotes com pausa entre lotes, com o TAMANHO DO LOTE e a DURACAO DA PAUSA parametrizados em constantes configuraveis (valores iniciais derivados do observado: ~14 chamadas / ~5s), de modo que o comportamento seja testavel e ajustavel sem alterar a logica.
- Erros transitorios 5xx usam backoff exponencial com teto; esgotado o numero de tentativas, a falha e registrada.
- 401 nao e re-tentado (credencial invalida) e gera erro classificado.

#### US-08 - Coletar processos das duas empresas
**Como** operador do cockpit, **quero** ingerir os processos das empresas famaha e darlu a partir da unica instancia Nomus, **para** ter a memoria operacional das duas empresas no substrato.
Referencia de codigo: decisao multi-empresa por campo `empresa` (uma fonte unica, sem multiplas instancias) em `getFonteByTipo`.
Criterios de aceite:
- CONFIRMADO: uma unica base + chave de integracao Nomus retorna os processos das DUAS empresas (famaha e darlu); o exemplo de URL por subdominio no discovery nao implica base/credencial por empresa. Mantem-se o modelo de fonte unica.
- A coleta usa uma unica fonte Nomus e uma unica chave de integracao.
- Cada processo persistido grava o campo `empresa` retornado pela API.
- O campo `empresa` discrimina a origem mas nao compoe a chave de deduplicacao.
- Processos de ambas as empresas aparecem no substrato apos a coleta.

### Dominio D: Persistencia no substrato

#### US-09 - Persistir processos em tabela propria com dedup
**Como** desenvolvedor do DLH Core, **quero** persistir os processos numa tabela `nomus_processos` com chave de dedup `nomus_id`, **para** nao forcar o dominio de processos dentro da tabela `avisos` (modelada por `effecti_id`).
Referencia de codigo: tabela `avisos` (`effecti_id UNIQUE`) em `20260606120100_tables.sql`; padrao de upsert `onConflict` em `runIncrementalSync`/`upsertBatch` e `persistAvisoBase` (`pipeline.ts`).
Criterios de aceite:
- Existe uma tabela `nomus_processos` com `nomus_id` UNIQUE NOT NULL como chave de deduplicacao.
- A tabela guarda os campos padrao expostos pelo GET (id, tipo, etapa, empresa, pessoa/cliente, descricao, nome, reportador, responsavel, datas) e o `payload_bruto` (jsonb integral).
- O upsert usa `onConflict: nomus_id`; reingestao do mesmo `nomus_id` sobrescreve com o estado vigente.
- A tabela tem PK UUID (`gen_random_uuid()`), RLS ativa no mesmo padrao das demais tabelas, e e auditada por trigger quando aplicavel.
- Campos personalizados do Nomus NAO sao persistidos (write-only, indisponiveis por GET).
- Anexos NAO sao persistidos nesta entrega.

#### US-10 - Manter o estado do processo atualizado
**Como** operador do cockpit, **quero** que reingerir um processo ja existente atualize seu estado (ex.: evolucao de `etapa`), **para** que o substrato reflita o snapshot vigente, nao apenas processos novos.
Referencia de codigo: controle `status_indexacao` e upsert idempotente em `pipeline.ts`/`embeddings.ts`.
Criterios de aceite:
- O upsert por `nomus_id` sobrescreve os campos com o estado atual a cada coleta.
- A reindexacao e decidida por comparacao de um HASH do conteudo textual canonico do processo (campos textuais definidos, sobretudo `descricao`, concatenados de forma deterministica). Se o hash mudar em relacao ao persistido, o processo e marcado para reindexacao (`status_indexacao` retorna a pendente/em_andamento e os chunks sao regenerados).
- Se o hash do conteudo textual canonico for igual ao persistido, nao ha reindexacao desnecessaria.
- O historico de evolucao de etapas NAO e persistido (apenas o estado vigente).

### Dominio E: Coleta em blocos com checkpoint e sync incremental

#### US-11 - Coletar em blocos com checkpoint e continuacao
**Como** desenvolvedor do DLH Core, **quero** que a coleta do Nomus rode em blocos com checkpoint persistido e se re-enfileire ate concluir, **para** ser robusta ao limite de tempo da Edge Function diante de uma base grande e com rate limit agressivo.
Referencia de codigo: `execucoes` (com `etapa_atual`, contadores) em `20260606120100_tables.sql`; padrao de execucao em `ingestao-coletar`/`pipeline.ts` (hoje materializa tudo em memoria, sem checkpoint).
Criterios de aceite:
- A tabela `execucoes` recebe, via migracao, as colunas necessarias ao checkpoint (ex.: `checkpoint` jsonb com pagina/cursor e contadores), pois hoje so possui `etapa_atual`, `total_processar`, `processados_sucesso`, `processados_erro` e `pendentes`, sem cursor de paginacao.
- Cada execucao da Edge Function processa um lote de paginas e persiste o progresso (pagina/cursor atual, contadores, estado) em `execucoes`.
- Ao atingir o limite do bloco, a continuacao e retomada pelo orquestrador via pg_cron existente (`ingestao-orquestrar`/`config_agendamento`), que no proximo tick avanca o checkpoint salvo, sem reprocessar paginas ja concluidas; NAO se cria fila nem mecanismo de auto-reinvocacao novo.
- O fluxo respeita o throttling do Nomus durante toda a continuacao.
- A execucao so e marcada como `concluida` quando todos os blocos da janela foram processados.
- Em falha de infraestrutura, a execucao reflete estado `erro` e o checkpoint permite diagnostico do ponto de parada.

#### US-12 - Sincronizar incrementalmente por data de ultima alteracao
**Como** operador do cockpit, **quero** que a janela incremental varra tanto processos novos quanto recem-alterados, **para** capturar atualizacoes de etapa, nao apenas criacoes.
Referencia de codigo: filtro de janela por data no `collect` do conector (analogo a `sinceDate`/`buildWindowBody`); janela default em `ingestao-coletar` (`DEFAULT_JANELA_DIAS = 7`).
Criterios de aceite:
- A janela inicial e de 7 dias.
- Quando a API expor um campo de ultima alteracao filtravel via `?query=campoData>yyyy-mm-ddTHH:mm:ss`, a janela usa esse campo (data de alteracao, nao de criacao).
- Quando o campo de ultima alteracao nao existir, o fallback re-varre periodicamente os processos nao finalizados (etapa nao terminal) alem da janela de novos.
- A janela e configuravel (mesma logica de `janela_dias` do `config_ingestao`).
- O design de blocos com checkpoint serve a janela incremental e prepara o caminho para backfill futuro sem reescrita.

### Dominio F: Indexacao semantica agnostica de origem

#### US-13 - Indexar o conteudo textual dos processos para busca semantica
**Como** Lia (consumidora do substrato), **quero** encontrar processos por similaridade textual, **para** recuperar memoria operacional relevante sobre vendas governamentais.
Referencia de codigo: `chunkText`/`generateAndStoreChunks` e provider plugavel (bge-m3, `vector(1024)`) em `_shared/embeddings.ts`; tabela `aviso_chunks` (HNSW) em `20260606120100_tables.sql`.
Criterios de aceite:
- O conteudo textual do processo (sobretudo `descricao`) e segmentado em chunks e indexado com embeddings.
- A indexacao reaproveita o provider plugavel existente (bge-m3, `vector(1024)`, indice HNSW `vector_cosine_ops`).
- Cada chunk carrega um discriminador de `origem`/`tipo` (ex.: `processo-venda-governamental`).
- O verbatim/payload integro do processo permanece preservado e nunca e mutado pela indexacao.
- A indexacao e idempotente: reindexar limpa os chunks antigos do processo antes de regravar.

#### US-14 - Indice de memoria agnostico de origem
**Como** desenvolvedor do DLH Core, **quero** que o indice semantico aceite chunks de qualquer recurso com discriminador de origem, **para** que novos recursos entrem so passando a ser indexados, sem nova RPC nem novo endpoint.
Referencia de codigo: `aviso_chunks` (hoje so de avisos) e RPC `busca_semantica_chunks` em `20260606121000_busca_semantica.sql`.
Criterios de aceite:
- Os chunks passam a viver num modelo agnostico de origem (tabela generica de chunks de memoria, ou union com discriminador) que convive com a busca de avisos ja em producao.
- A busca de avisos atualmente em producao continua funcionando sem regressao.
- Um chunk de processo so e retornado em consultas semanticamente proximas; nao polui consultas sobre editais.
- Adicionar um recurso futuro (ex.: cobranca) ao indice nao exige nova RPC nem novo endpoint de busca.

#### US-15 - Busca unificada com filtro de escopo
**Como** Lia (consumidora do substrato), **quero** buscar memoria por significado em todas as origens, com filtro opcional de escopo, **para** recuperar resultados marcados com sua origem (aviso, processo, etc.).
Referencia de codigo: endpoint `v1-substrato-busca-semantica/index.ts` e RPC `busca_semantica_chunks`.
Criterios de aceite:
- A busca retorna resultados de multiplas origens, cada um marcado com seu metadado de origem/tipo.
- A busca aceita um filtro de escopo OPCIONAL (tudo / so processos / so avisos / por tipo).
- Sem filtro de escopo, a busca e federada entre todas as origens indexadas.
- A autenticacao `/v1` existente (API key da Lia no Vault OU sessao do cockpit) e mantida.
- A generalizacao da RPC `busca_semantica_chunks` (hoje retorna `aviso_id` + `verbatim` com join obrigatorio a `avisos`) DEVE preservar a compatibilidade do contrato de resposta consumido pela Lia: o identificador de origem e ADITIVO (ex.: `origem` + `registro_id` generico), sem remover/renomear o campo hoje retornado, ou a resposta e versionada. O join passa a ser origem-aware (union/left join + `origem`), nao mais fixo em `avisos`.
- A correlacao explicita processo <-> aviso NAO e implementada (busca unificada nao implica vinculo entre registros).

### Dominio G: Orquestracao e agendamento

#### US-16 - Disparar coleta do Nomus sob demanda
**Como** operador do cockpit, **quero** disparar manualmente a coleta da fonte Nomus pela UI, **para** atualizar o substrato quando quiser.
Referencia de codigo: `ingestao-coletar/index.ts` (gatilho manual, anti-duplo-disparo) e `ColetaButton` em `execucoes-client.tsx`.
Criterios de aceite:
- O disparo generaliza o `ingestao-coletar` existente parametrizando `fonte` e `recurso` no `coletarSchema` e no handler (hoje fixo em `effecti`: `getFonteByTipo()` sem parametro e mensagem "credencial Effecti"), resolvendo `getFonteByTipo(fonte)` e com mensagem de erro de credencial generica por fonte; NAO se cria funcao nova por fonte.
- A UI permite disparar a coleta escolhendo a fonte/recurso (Nomus / processos) e respeita os tipos ativos configurados.
- O disparo manual cria uma execucao `em_andamento` e retorna imediatamente, com progresso refletido via Realtime de `execucoes`.
- O disparo respeita a trava single-flight: nao inicia se ja houver execucao em andamento.
- A acao e auditada em `audit_log`.

#### US-17 - Agendamento sequencial single-flight para multiplas fontes
**Como** operador do cockpit, **quero** que o agendamento automatico rode uma fonte/recurso por vez, sem coletas concorrentes, **para** preservar o rate limit do Nomus e evitar sobreposicao.
Referencia de codigo: `ingestao-orquestrar/index.ts` (relogio global, anti-sobreposicao, `runCycle` por ordem) e `config_agendamento` + `fontes.ativa`/`fontes.ordem` em `20260606130000_agendamento_global.sql`.
Criterios de aceite:
- Ha um unico orquestrador que escolhe o proximo recurso pendente e avanca seu checkpoint, passando ao proximo ao concluir.
- No maximo uma execucao ativa por vez (trava single-flight por estado em `execucoes`); um tique com execucao anterior em andamento nao inicia outra.
- Os incrementais de todas as fontes tem prioridade e rodam primeiro, na ordem cadastrada (`fontes.ordem`).
- O backfill (longo) roda em faixa de menor prioridade, preenchendo janelas ociosas, sem bloquear os incrementais.
- O agendamento automatico e OPCIONAL e reaproveita o mecanismo de pg_cron / `config_agendamento` ja existente, sem criar um agendador novo.

### Dominio H: UI e observabilidade no cockpit

#### US-18 - Bloco da fonte Nomus na tela de Fontes
**Como** operador do cockpit, **quero** ver e gerenciar a fonte Nomus na tela de Fontes, **para** cadastrar/testar credencial, selecionar recursos/tipos e disparar coleta.
Referencia de codigo: `fontes/page.tsx`, `fonte-effecti-block.tsx` (hoje lista "ERP Nomus" como fonte futura desabilitada), `cred-form.tsx`, `cfg-form.tsx`.
Criterios de aceite:
- A tela de Fontes passa a exibir um bloco ativo da fonte Nomus (sai da lista "Fontes futuras" desabilitadas).
- O bloco permite: cadastrar/testar a credencial, selecionar recursos ativos e tipos por recurso, e disparar coleta manual.
- Nesta entrega, vem ativo apenas o recurso processos + tipo "Venda Governamental"; os demais recursos aparecem visiveis e desligados.
- A inclusao das novas superficies passa pela revisao do Design Lock (`manifest.json locked:true`) sem quebrar as telas existentes travadas.
- As telas e itens de menu travados pelo Design Lock nao sao alterados sem destravar o lock.

#### US-19 - Monitorar execucoes e erros por origem e recurso
**Como** operador do cockpit, **quero** ver execucoes e erros discriminados e filtraveis por origem (Effecti x Nomus) e por recurso/tipo, **para** monitorar a fonte Nomus com a mesma paridade do Effecti.
Referencia de codigo: `execucoes-client.tsx`, `erros-client.tsx`, `runs-table.tsx`, `erros-table.tsx`, tabelas `execucoes` e `erros_ingestao`.
Criterios de aceite:
- A tabela `execucoes` recebe, via migracao, coluna de origem/fonte e de recurso/tipo (hoje a `execucoes` nao referencia `fontes` nem registra recurso), permitindo o filtro multi-origem.
- A tabela `erros_ingestao` recebe, via migracao, coluna de origem/recurso e uma referencia generica ao registro de origem (ex.: `registro_id` + `origem`), pois hoje so possui `aviso_id` (FK exclusiva a `avisos`), que nao serve a um erro de processo Nomus.
- As telas de execucoes e erros passam a discriminar e filtrar por origem (Effecti x Nomus) e por recurso/tipo.
- O historico de execucoes da fonte Nomus mostra estado, progresso/checkpoint, contadores e horarios.
- Os erros de ingestao da fonte Nomus aparecem por origem/recurso com severidade e etapa.
- O estado/saude da fonte Nomus (ultima coleta, sucesso/falha) e visivel, em paridade com o Effecti.
- A apresentacao reutiliza as tabelas `execucoes` e `erros_ingestao` existentes; o que muda e a generalizacao da apresentacao para multi-origem.

---

## 5. Requisitos Funcionais (RF)

> Requisitos aprovados, mantidos intactos do documento de stories e requisitos.

### Dominio A: Conexao e credencial

- **RF-00** (US-00): O sistema DEVE provisionar, via migracao/seed idempotente, a fonte `nomus` em `fontes` (`tipo='nomus'`, `endpoint_base`, `estado_conexao='nao_configurada'`, `ativa`, `ordem`) e sua `config_ingestao` (janela default 7 dias e suporte a data inicial especifica), no padrao do seed do Effecti. Sem esse registro, `getFonteByTipo("nomus")` retorna 404.
- **RF-01** (US-01): O sistema DEVE gravar a chave de integracao do Nomus no Supabase Vault via RPC `set_fonte_secret`, persistindo apenas a referencia em `fontes.token_cifrado`. Integra-se a `_shared/vault.ts` (`setFonteSecret`).
- **RF-02** (US-01): O endpoint de credencial DEVE validar o corpo com zod (token nao-vazio) e exigir `requireAuthorizedUser` antes de processar o corpo, no padrao de `fontes-credencial/index.ts`.
- **RF-03** (US-02): O sistema DEVE testar a conexao com o Nomus lendo a chave do Vault em runtime e fazendo uma requisicao leve a `GET /rest/processos`, atualizando `fontes.estado_conexao` para `conectada`/`erro`/`nao_configurada`.
- **RF-04** (US-02): O teste de conexao DEVE classificar a causa da falha em `unauthorized` (401), `rate_limited` (429), `timeout` e `unknown`, retornando mensagem distinta por causa e a latencia em ms, no padrao de `ConnectionTestError`.
- **RF-05** (US-03): Os endpoints de credencial e teste DEVEM resolver a fonte pelo tipo informado (`nomus`), usando `getFonteByTipo(tipo)` (que ja aceita o parametro), sem path fixo `effecti`, preservando o comportamento atual do Effecti.

### Dominio B: Configuracao de recursos e tipos

- **RF-06** (US-04): O sistema DEVE persistir a lista de recursos ativos numa NOVA COLUNA `jsonb` de `config_ingestao` (ex.: `recursos`), com apenas `processos` ativo nesta entrega, pois `modalidades`/`portais` (`text[] NOT NULL`) sao especificos do Effecti; a config Nomus preenche `modalidades`/`portais` vazios (`'{}'`).
- **RF-07** (US-05): O sistema DEVE persistir, por recurso, a lista de tipos ativos DENTRO da mesma estrutura `jsonb` (mapa recurso->tipos[]), com apenas `Venda Governamental` ativo para o recurso `processos`, ja que um `text[]` plano nao representa tipos-por-recurso.
- **RF-08** (US-04, US-05): A selecao de recursos e tipos ativos DEVE governar tanto a coleta manual quanto a agendada, valendo na proxima execucao sem redeploy (padrao de `ingestao-config`).
- **RF-09** (US-05): O conector DEVE aplicar o filtro de `tipo` no cliente, sobre o array retornado pela API, descartando processos fora da allowlist antes da persistencia, no padrao de allowlist do `EffectiConnector.collect`.

### Dominio C: Conector e coleta

- **RF-10** (US-06): O conector Nomus DEVE autenticar com header `Authorization: Basic <chave>` + `Content-Type: application/json` e paginar `GET /rest/processos?pagina=N` de forma incremental a partir de 1, tratando a resposta como array.
- **RF-11** (US-06): A factory `createConnector` DEVE suportar o tipo `nomus` retornando o conector Nomus, sem alterar o caso `effecti`, mantendo o contrato `SourceConnector` desacoplado de `fontes.tipo`.
- **RF-12** (US-06): O contrato de dados do conector DEVE ser generalizado para acomodar um "registro coletado" de processo Nomus (campos padrao + payload bruto), ja que `CollectedAviso` nao comporta um processo. O Nomus usa contrato/segmento proprio ou um tipo generico de coleta.
- **RF-13** (US-07): O conector DEVE respeitar HTTP 429 aguardando `tempoAteLiberar` antes de re-tentar, processar em lotes com pausa entre lotes usando TAMANHO DE LOTE e PAUSA parametrizados em constantes configuraveis (defaults documentados: ~14 chamadas / ~5s), aplicar backoff exponencial com teto em 5xx, e nao re-tentar 401, no padrao de `fetchWithBackoff`/`parseRetryAfter`/`computeBackoff`.
- **RF-14** (US-08): A coleta DEVE usar uma unica fonte e chave Nomus e gravar o campo `empresa` de cada processo, ingerindo as empresas famaha e darlu, sem multiplas instancias de fonte. CONFIRMADO que uma unica base+chave retorna as duas empresas (o subdominio por empresa no exemplo da API nao exige fonte/credencial por empresa).

### Dominio D: Persistencia

- **RF-15** (US-09): O sistema DEVE criar a tabela `nomus_processos` com `nomus_id` UNIQUE NOT NULL (chave de dedup), PK UUID `gen_random_uuid()`, campos padrao do GET, `payload_bruto` jsonb integral e coluna de controle `status_indexacao`, no padrao de `avisos`.
- **RF-16** (US-09): A tabela `nomus_processos` DEVE ter RLS ativa na mesma policy do MVP (`is_conta_autorizada`) e ser coberta por triggers de auditoria/`updated_at` no padrao das demais tabelas.
- **RF-17** (US-09, US-10): A persistencia DEVE fazer upsert com `onConflict: nomus_id`, sobrescrevendo o estado vigente do processo a cada coleta, no padrao de `runIncrementalSync`/`persistAvisoBase` adaptado para `nomus_processos`.
- **RF-18** (US-09): O sistema NAO DEVE persistir campos personalizados (write-only, ausentes em qualquer GET) nem anexos nesta entrega.
- **RF-19** (US-10): Ao reingerir um processo, o sistema DEVE disparar reindexacao (regerar chunks/embeddings) quando o HASH do conteudo textual canonico (campos textuais definidos, sobretudo `descricao`, concatenados deterministicamente) mudar em relacao ao persistido, e evitar reindexacao quando o hash for igual, controlando por `status_indexacao`.

### Dominio E: Coleta em blocos e sync incremental

- **RF-20** (US-11): A coleta DEVE processar blocos de paginas por execucao da Edge Function, persistir o checkpoint (pagina/cursor, contadores, estado) em `execucoes`, INCLUINDO a migracao das colunas de checkpoint necessarias (a tabela atual nao possui coluna de pagina/cursor), e a continuacao DEVE ser retomada pelo orquestrador via pg_cron existente (`ingestao-orquestrar`/`config_agendamento`) ate concluir, sem fila nova nem auto-reinvocacao, sem reprocessar paginas concluidas.
- **RF-21** (US-11): A execucao so DEVE ser marcada `concluida` apos todos os blocos da janela; falha de infraestrutura DEVE refletir estado `erro` preservando o checkpoint para diagnostico.
- **RF-22** (US-12): A janela incremental DEVE usar o filtro de data da API (`?query=campoData>yyyy-mm-ddTHH:mm:ss`) operando sobre a data de ultima alteracao, com janela default de 7 dias configuravel via `janela_dias`.
- **RF-23** (US-12): Caso a API nao exponha um campo de ultima alteracao filtravel, o sistema DEVE aplicar o fallback de re-scan periodico dos processos nao finalizados (etapa nao terminal), alem da janela de novos. (Dependencia tecnica a confirmar na fase de spec.)

### Dominio F: Indexacao semantica

- **RF-24** (US-13): O sistema DEVE segmentar o conteudo textual do processo (sobretudo `descricao`) e gerar embeddings via provider plugavel existente (bge-m3, `vector(1024)`, HNSW `vector_cosine_ops`), de forma idempotente, no padrao de `chunkText`/`generateAndStoreChunks`.
- **RF-25** (US-14): O indice de chunks DEVE ser agnostico de origem, com discriminador `origem`/`tipo` por chunk, convivendo com a busca de avisos em producao sem regressao (migrar `aviso_chunks` para o modelo agnostico ou criar tabela generica que coexista).
- **RF-26** (US-15): O endpoint de busca semantica DEVE retornar resultados de multiplas origens marcados por origem/tipo e aceitar um filtro de escopo OPCIONAL (tudo / processos / avisos / por tipo), generalizando a RPC `busca_semantica_chunks` e o endpoint `v1-substrato-busca-semantica` sem novo endpoint por recurso, PRESERVANDO a compatibilidade do contrato de RESPOSTA para a Lia (campo de origem aditivo + `registro_id` generico; sem breaking no campo atualmente retornado) e tornando o join origem-aware em vez de fixo em `avisos`.
- **RF-27** (US-15): O sistema NAO DEVE criar vinculo explicito processo <-> aviso; a busca unificada e independente de correlacao entre registros.

### Dominio G: Orquestracao e agendamento

- **RF-28** (US-16): O sistema DEVE permitir disparo manual da coleta da fonte Nomus (recurso processos), criando execucao `em_andamento` com retorno imediato e progresso via Realtime de `execucoes`, GENERALIZANDO o `ingestao-coletar` existente (parametrizar `fonte` e `recurso` no `coletarSchema` e no handler, hoje fixos em `effecti`), sem criar funcao nova por fonte.
- **RF-29** (US-16, US-17): O sistema DEVE manter trava single-flight global: no maximo uma execucao ativa por vez; novo disparo (manual ou agendado) com execucao em andamento NAO inicia outra coleta, no padrao anti-sobreposicao de `ingestao-coletar`/`ingestao-orquestrar`.
- **RF-30** (US-17): O orquestrador DEVE percorrer as fontes/recursos ativos em ordem (`fontes.ordem`), coletando um por vez e avancando o checkpoint, reaproveitando `config_agendamento` e o pg_cron existentes, sem criar agendador novo.
- **RF-31** (US-17): O orquestrador DEVE priorizar os incrementais de todas as fontes (rodam primeiro) e rodar o backfill em faixa de menor prioridade, preenchendo janelas ociosas, com ordem/prioridade configuravel.

### Dominio H: UI e observabilidade

- **RF-32** (US-18): A tela de Fontes DEVE exibir um bloco ativo da fonte Nomus com cadastro/teste de credencial, selecao de recursos ativos e tipos por recurso, e disparo de coleta manual, no padrao de `fonte-effecti-block.tsx`/`cred-form.tsx`/`cfg-form.tsx`.
- **RF-33** (US-18): A inclusao das novas superficies de UI DEVE passar pela revisao/destrave do Design Lock (`manifest.json locked:true`) sem quebrar as telas existentes travadas (`src/lib/nav.ts`).
- **RF-34** (US-19): As telas de execucoes e erros DEVEM discriminar e filtrar por origem (Effecti x Nomus) e por recurso/tipo, reutilizando `execucoes` e `erros_ingestao` APOS migracao que adiciona, em ambas, as colunas de origem/fonte e recurso (hoje inexistentes) e, em `erros_ingestao`, uma referencia generica ao registro de origem em vez do `aviso_id` exclusivo a `avisos`, e os componentes `runs-table.tsx`/`erros-table.tsx`.
- **RF-35** (US-19): O cockpit DEVE expor o estado/saude da fonte Nomus (ultima coleta, sucesso/falha, progresso/checkpoint) em paridade com o Effecti, reaproveitando o padrao de observabilidade existente.

---

## 6. Requisitos Nao-Funcionais (RNF)

> Requisitos aprovados, mantidos intactos do documento de stories e requisitos.

### Seguranca

- **RNF-01**: A chave de integracao do Nomus DEVE residir apenas como segredo no Supabase Vault (referencia em `fontes.token_cifrado`), nunca em texto pleno, nunca em `.env` de producao, nunca retornada ao cliente e nunca registrada em `audit_log` (apenas referencia/fonte). Defesa em profundidade: autorizacao na borda (`requireAuthorizedUser`) + RLS.
- **RNF-02**: Todo acesso de escrita ao substrato pela coleta DEVE usar `service_role` apenas server-side; a RPC de busca semantica permanece `SECURITY DEFINER` executavel so por `service_role`, com autorizacao garantida na borda (`authenticateV1`).
- **RNF-03**: A tabela `nomus_processos` e o indice de chunks DEVEM ter RLS ativa na policy `is_conta_autorizada`, em paridade com as demais tabelas do MVP.

#### Decisoes de Seguranca da feature Nomus (consolidadas)

> Decisoes aprovadas pelo agente de Security. Seguem o MODELO EXISTENTE do DLH Core (Supabase Auth + Google OAuth, perfil unico `interno`; autorizacao na borda `requireAuthorizedUser`/`authenticateV1`; RLS unica `is_conta_autorizada()`; segredos no Vault; auditoria via `logSensitiveAction`). NENHUM modelo de autenticacao e trocado e NENHUM mecanismo existente e removido.

- **SEC-01 (Credencial Nomus no Vault):** A chave de integracao REST do Nomus (header `Authorization: Basic <chave>`) DEVE ser gravada via RPC `set_fonte_secret` (Vault), com `fontes.token_cifrado` guardando apenas a referencia. Nunca em `.env` de producao, nunca retornada ao cliente, nunca registrada em log ou `audit_log`, e o header `Authorization: Basic` NUNCA pode ser logado. Lida em runtime via `getFonteSecret`, sem persistir fora do Vault. (Reforca RNF-01.)
- **SEC-02 (Autorizacao na borda dos endpoints parametrizados):** Os endpoints que deixam de ser fixos em `effecti` e passam a receber `fonte`/`recurso` (`fontes-credencial`, `fontes-testar`, `ingestao-coletar`, `ingestao-config`) DEVEM exigir `requireAuthorizedUser` ANTES de processar o corpo, mantendo o padrao atual de autorizacao na borda.
- **SEC-03 (Allowlist estrita de `fonte`/`recurso`):** O parametro `fonte`/`recurso` DEVE ser validado contra um conjunto conhecido via zod enum (`fonte` em {`effecti`, `nomus`}; `recurso` em {`processos`}) ANTES de qualquer resolucao ou I/O. Valor desconhecido retorna erro de validacao (422) ou 404, sem chamar a API externa. Fecha a superficie ampliada pela parametrizacao e evita resolucao arbitraria de fonte.
- **SEC-04 (RLS das novas tabelas):** `nomus_processos` E a tabela de chunks agnostica de origem (seja `aviso_chunks` migrada ou tabela generica nova) DEVEM ter RLS habilitada `for all` com `USING (is_conta_autorizada())` + `WITH CHECK (is_conta_autorizada())`, identica as 9 tabelas atuais, preservando o deny-by-default. (Detalha RNF-03; cobre a lacuna L-03 do ponto de vista de RLS: qualquer que seja o modelo de chunks escolhido no design, a policy de RLS e obrigatoria.)
- **SEC-05 (service_role apenas server-side):** Toda escrita no substrato pela coleta (upsert em `nomus_processos`, gravacao de chunks/embeddings, `execucoes`, `erros_ingestao`) DEVE usar `service_role` exclusivamente server-side (Edge Functions). A `service_role` nunca e exposta ao cliente. (Reforca RNF-02.)
- **SEC-06 (Busca semantica /v1 generalizada):** A RPC `busca_semantica_chunks`, ao se tornar multi-origem, PERMANECE `SECURITY DEFINER` executavel apenas por `service_role`, e o endpoint `v1-substrato-busca-semantica` MANTEM `authenticateV1` na borda. A API key da Lia continua read-only (escopo `read-only:busca-semantica`), distinta da `service_role` e da sessao humana, rotacionavel/revogavel via Vault. O filtro de escopo opcional NAO cria nova fronteira de autorizacao (todas as origens estao sob a mesma allowlist).
- **SEC-07 (Sem segregacao por empresa):** O campo `empresa` (famaha/darlu) e apenas discriminador de origem, NAO fronteira de autorizacao. Mantem-se o perfil unico `interno` do MVP: o usuario autorizado enxerga as duas empresas. Nao se introduz permissao por empresa nesta entrega.
- **SEC-08 (Auditoria das novas acoes):** DEVEM ser auditadas via `logSensitiveAction`, sempre SEM o segredo e SEM o header Basic: (a) cadastro/rotacao da credencial Nomus (apenas referencia/fonte), (b) teste de conexao Nomus (acao e causa do resultado), (c) disparo de coleta manual (quem/quando, fonte/recurso), (d) toggle de recursos/tipos ativos em `config_ingestao` (alteracao de escopo da coleta).
- **SEC-09 (Nao vazar payload em erro):** A coluna generica de origem em `erros_ingestao` (`registro_id` + `origem`, substituindo o `aviso_id` exclusivo) NAO DEVE armazenar o payload bruto do processo Nomus nem dados de negocio sensiveis; registra apenas identificador, origem/recurso, severidade e etapa. (Coerente com o padrao de auditoria sem vazar conteudo.)
- **SEC-10 (CORS inalterado):** A configuracao de CORS existente (`_shared/cors.ts`) e mantida como esta; esta feature nao altera origens, metodos nem cabecalhos permitidos.

### Confiabilidade

- **RNF-04**: A coleta DEVE ser robusta ao limite de tempo da Edge Function via blocos com checkpoint, com a retomada feita pelo orquestrador via pg_cron existente (sem fila nova nem auto-reinvocacao), garantindo retomada exata sem reprocessar paginas ja concluidas nem perder paginas pendentes.
- **RNF-05**: Falha isolada de um processo (tratamento/indexacao) NAO DEVE derrubar o lote: deve virar registro em `erros_ingestao` (severidade/etapa) usando a referencia generica de origem (nao o `aviso_id`, que so aponta para `avisos`) e a coleta continua, no padrao de isolamento de falha por item do `pipeline.ts`.
- **RNF-06**: O conector DEVE respeitar o `tempoAteLiberar` do HTTP 429 do Nomus e usar backoff exponencial com teto em 5xx, garantindo que nenhuma coleta concorrente rode em paralelo (single-flight), preservando o rate limit.
- **RNF-07**: A deduplicacao por `nomus_id` DEVE ser estavel e nao colidir entre as empresas; reingestao do mesmo `nomus_id` produz exatamente uma linha com o estado vigente.

### Performance

- **RNF-08**: A busca semantica multi-origem DEVE usar o indice HNSW (`vector_cosine_ops`) existente sobre vetores `vector(1024)`; o filtro de escopo opcional nao deve impedir o uso do indice vetorial.
- **RNF-09**: A geracao de embeddings da ingestao DEVE usar o provider self-hosted plugavel (bge-m3), com ZERO custo por token, sem nunca usar o modelo Claude na ingestao.
- **RNF-10**: O custo total do ciclo sequencial e aceito como a soma das fontes; janelas incrementais curtas (7 dias) DEVEM manter o custo baixo, e o backfill DEVE rodar em prioridade inferior para nao atrasar os incrementais (evitar starvation).

### Usabilidade

- **RNF-11**: O progresso da coleta Nomus DEVE refletir ao vivo via Realtime de `execucoes` (com fallback de poll), em paridade com o Effecti.
- **RNF-12**: As mensagens de erro de teste de conexao e de coleta DEVEM ser especificas por causa (credencial invalida, rate limit, timeout, indisponibilidade), no padrao de copy por causa ja adotado para o Effecti.
- **RNF-13**: As novas superficies de UI DEVEM respeitar o Design Lock vigente: nenhuma nova tela, item de menu, componente ou estado fora do lock sem revisao/destrave formal do `manifest.json`.

### Manutenibilidade e extensibilidade

- **RNF-14**: O design do conector e da configuracao DEVE acomodar recursos e tipos futuros (cobranca, propostas, pedidos, NFes, contas a receber) ligaveis por toggle, sem refatorar o conector a cada novo recurso/tipo.
- **RNF-15**: O modelo do substrato DEVE seguir uma tabela por recurso Nomus (`nomus_processos` agora; demais depois), com ingestao independente por recurso e dedup por id do recurso.
- **RNF-16**: A generalizacao do indice de chunks e da RPC/endpoint de busca DEVE permitir que novos recursos entrem apenas passando a ser indexados, sem nova RPC nem novo endpoint de busca.
- **RNF-17**: O reuso do Effecti limita-se ao PADRAO (paginacao + backoff + sync incremental + dedup), nao ao codigo literal: o sync e o contrato de dados DEVEM ser proprios do Nomus ou generalizados, ja que `runIncrementalSync`/`upsertBatch`/`CollectedAviso` sao acoplados ao dominio de avisos. A introducao do Nomus NAO DEVE causar regressao nos fluxos do Effecti.

---

## 7. Metricas de Sucesso

> KPIs que indicam se a feature esta funcionando conforme o esperado. Derivados das stories e requisitos aprovados.

### Cobertura de ingestao
- 100% das paginas de `/rest/processos` da janela varridas por execucao concluida (nenhuma pagina pendente nem reprocessada), comprovado pelo checkpoint em `execucoes` (RF-20, RF-21).
- Processos das DUAS empresas (famaha e darlu) presentes no substrato apos a coleta, com o campo `empresa` corretamente gravado (RF-14).
- Zero processos com `tipo` fora da allowlist ("Venda Governamental") persistidos (RF-09).

### Atualidade e integridade do estado
- Reingestao do mesmo `nomus_id` resulta em exatamente UMA linha com o estado vigente (sem duplicatas), confirmando a dedup estavel (RNF-07).
- Processos recem-alterados (evolucao de etapa) refletidos no substrato dentro da janela incremental, nao apenas os novos (RF-22, RF-23).
- Reindexacao disparada apenas quando o hash do conteudo textual canonico muda; zero reindexacoes desnecessarias quando o hash e igual (RF-19).

### Confiabilidade da coleta
- Coleta robusta ao timeout da Edge Function: execucoes longas concluem via continuacao por checkpoint sem perda de paginas (RNF-04).
- Respeito ao rate limit do Nomus: zero bloqueios persistentes por 429; o conector aguarda `tempoAteLiberar` e nunca roda coletas concorrentes (RNF-06).
- Falha isolada de processo registrada em `erros_ingestao` sem derrubar o lote (taxa de continuidade do lote = 100% diante de erro por item) (RNF-05).

### Busca semantica
- A busca de avisos em producao continua funcionando sem regressao apos a generalizacao do indice (RF-25).
- A busca unificada retorna resultados de processos marcados com sua origem e respeita o filtro de escopo opcional (RF-26).
- O contrato de resposta consumido pela Lia permanece compativel (mudanca aditiva, sem breaking) (RF-26).

### Observabilidade e seguranca
- A fonte Nomus monitoravel no cockpit em paridade com o Effecti: estado/saude, historico de execucoes com progresso/checkpoint, e erros por origem/recurso visiveis e filtraveis (RF-34, RF-35, RNF-11).
- Zero ocorrencias da chave de integracao em respostas ao cliente, logs ou `audit_log` (RNF-01).

---

## 8. Escopo Negativo

> O que NAO faz parte desta entrega (registrado, nao implementar agora).

- **Campos personalizados do Nomus:** sao write-only na API (gravaveis via PUT, mas nunca retornados em nenhum GET). Nao serao ingeridos nem persistidos. A informacao de negocio correspondente (numero do edital, UASG, orgao, portal, prazos) nasce do edital/aviso e ja vive no lado do Effecti/substrato (RF-18).
- **Anexos:** tanto do Nomus quanto do Effecti ficam para feature posterior. Inclui o encadeamento conhecido em que o edital vindo do Effecti e anexado no proprio processo do Nomus (material da futura feature de anexos).
- **Correlacao explicita processo <-> aviso:** nao ha identificador unico ligando processo e aviso hoje; a ligacao depende de estrategia de correlacao por similaridade de campos, adiada para feature posterior. A busca unificada NAO implica vinculo entre registros (RF-27).
- **Escrita de volta no Nomus:** cadastro de avisos/processos no Nomus (POST/PUT) fica fora desta entrega, prevista para fase posterior (automacao de analise de editais).
- **Backfill historico:** apenas o design de blocos com checkpoint deve acomodar o backfill futuro; a janela inicial e de 7 dias. Puxar processos mais antigos fica para depois.
- **Demais recursos e tipos do Nomus:** cobranca, propostas, pedidos, NFes, contas a receber e os demais `tipo` de processos (ex.: "Cobranca") ficam INATIVOS nesta entrega, visiveis e desligados na UI, com o design ja preparado para ativacao futura por toggle sem reescrita.
- **Historico de evolucao de etapas:** guarda-se apenas o estado vigente do processo (snapshot); o historico de transicoes de etapa nao e persistido.

---

## 9. Dependencias e Riscos

### Dependencias com codigo existente
- `_shared/effecti-connector.ts`: interface `SourceConnector`, factory `createConnector`, e utilitarios de throttling (`fetchWithBackoff`, `parseRetryAfter`, `computeBackoff`). A factory e generica, mas o CONTRATO DE DADOS (`CollectedAviso`) e acoplado ao dominio de avisos e precisa ser generalizado para o Nomus.
- `_shared/vault.ts`: `getFonteByTipo(tipo)` (ja aceita o parametro, default `effecti`) e `setFonteSecret` para a credencial no Vault.
- `_shared/embeddings.ts`: provider plugavel de embeddings (bge-m3, `vector(1024)`, HNSW), `chunkText`, `generateAndStoreChunks`.
- `pipeline.ts`: padrao de persistencia, controle `status_indexacao` e isolamento de falha por item.
- Tabelas `fontes`, `config_ingestao`, `execucoes`, `erros_ingestao`, `aviso_chunks`, e a RPC `busca_semantica_chunks`.
- pg_cron / `config_agendamento` e orquestrador `ingestao-orquestrar` para retomada e agendamento.
- Componentes de UI: `fontes/page.tsx`, `fonte-effecti-block.tsx`, `cred-form.tsx`, `cfg-form.tsx`, `execucoes-client.tsx`, `erros-client.tsx`, `runs-table.tsx`, `erros-table.tsx`, e `src/lib/nav.ts` (Design Lock).

### Lacunas a confirmar na proxima fase
- **L-01** (RF-22/RF-23): Confirmar se o `GET /rest/processos` expoe um campo de ULTIMA ALTERACAO filtravel via `?query=campoData>...`. Se sim, a janela incremental usa esse campo; se nao, vale o fallback de re-scan dos processos nao finalizados.
- **L-02**: Detalhes operacionais da referencia externa "aiox" (auth, endpoints, tratamento de campos personalizados) nao estao no repositorio `dlh-core` e precisam ser fornecidos pelo usuario.
- **L-03** (RF-25): Decidir, na fase de design, entre migrar `aviso_chunks` para o modelo agnostico de origem ou criar tabela generica de chunks que coexista, preservando a busca de avisos ja em producao.
- **L-04** (RF-33): Escopo exato do destrave do Design Lock para as novas superficies da fonte Nomus a ser definido na fase de design.
- **L-05** (US-15/RF-26): Confirmar o contrato de resposta exato hoje consumido pela Lia em `v1-substrato-busca-semantica` (campos usados), para garantir que a generalizacao multi-origem seja aditiva e nao quebre o consumidor.

### Riscos tecnicos
- **Generalizacao do contrato de dados do conector:** `CollectedAviso` nao comporta um processo Nomus. Esperar reuso 1:1 do `runIncrementalSync`/`upsertBatch` e incoerente com o codigo atual; o que se reaproveita e o PADRAO, nao o codigo literal. Risco de regressao no Effecti se a generalizacao nao for cuidadosa (mitigado por RNF-17).
- **Generalizacao do indice semantico em producao:** migrar/coexistir `aviso_chunks` com o modelo agnostico exige preservar a busca de avisos ja em producao sem regressao e manter o contrato de resposta da Lia aditivo (mitigado por RF-25, RF-26, L-03, L-05).
- **Limite de tempo da Edge Function x rate limit agressivo:** a base tem ~720+ paginas e o Nomus tem throttling agressivo (lotes ~14 + pausa ~5s; 429 com `tempoAteLiberar`). Risco de timeout mitigado pela coleta em blocos com checkpoint e continuacao via orquestrador (RF-20, RF-21, RNF-04).
- **Campo de ultima alteracao incerto na API:** se a API nao expor o filtro por data de alteracao, a janela incremental nao captura updates por data, exigindo o fallback de re-scan dos nao finalizados (L-01, RF-23).
- **Design Lock:** as novas superficies de UI exigem revisao/destrave formal do `manifest.json locked:true` sem quebrar as telas travadas (RF-33, RNF-13, L-04).
- **Migracoes de tabelas existentes:** `execucoes` e `erros_ingestao` precisam de novas colunas (checkpoint, origem/fonte, recurso, referencia generica ao registro em vez do `aviso_id` exclusivo). Risco de impacto nos fluxos atuais do Effecti se as migracoes nao forem compativeis com os dados existentes (mitigado por RF-20, RF-34).
- **Dependencia de informacao externa (aiox):** detalhes operacionais nao estao no repositorio e dependem do usuario, podendo bloquear ou atrasar a fase de spec (L-02).

---

## 10. Decisoes Tecnicas

### Database

> Decisoes de persistencia/substrato consolidadas e aprovadas pelo usuario (2026-06-06).
> Banco existente: PostgreSQL no Supabase, schema `public`, migrations versionadas em `supabase/migrations/`. Extensoes ja habilitadas: `pgvector`, `pgcrypto`, `pg_cron`, `supabase_vault`.
> Principio geral: reaproveitar os padroes existentes do substrato (PK `uuid default gen_random_uuid()`, RLS via policy unica `is_conta_autorizada()`, triggers `fn_audit_log()` e `fn_set_updated_at()`, busca HNSW `vector_cosine_ops` sobre `vector(1024)`). NENHUMA tabela existente e recriada; alteracoes sao sempre ALTER aditivo. Nenhuma troca de banco.

#### D-01: Nova tabela `nomus_processos` (RF-15/16/17/18, RNF-03/07/15)
- CREATE TABLE `public.nomus_processos`, uma tabela por recurso Nomus (padrao extensivel: `nomus_propostas`, `nomus_pedidos`, etc. no futuro).
- Colunas:
  - `id uuid primary key default gen_random_uuid()`
  - `nomus_id text unique not null` (chave de deduplicacao; estavel na instancia, nao colide entre empresas)
  - `tipo text` (ex.: "Venda Governamental")
  - `etapa text`
  - `empresa text` (discrimina famaha/darlu; NAO compoe a chave)
  - `pessoa text` (cliente)
  - `nome text`
  - `descricao text` (principal conteudo textual indexado)
  - `reportador text`
  - `responsavel text`
  - `data_inicial timestamptz`, `data_final timestamptz` (datas padrao expostas pelo GET; nomes finais ajustaveis na spec conforme o payload real)
  - `data_ultima_alteracao timestamptz` (suporte ao sync incremental por alteracao, dependente de L-01)
  - `payload_bruto jsonb not null` (payload integral do GET, sem descarte)
  - `conteudo_hash text` (hash do conteudo textual canonico para decidir reindexacao; ver D-02)
  - `status_indexacao text` (controle do pipeline de embeddings, padrao de `avisos`)
  - `created_at timestamptz default now()`
  - `updated_at timestamptz`
- Campos personalizados (write-only na API) e anexos NAO sao persistidos nesta entrega (RF-18).
- Upsert com `onConflict: nomus_id`, sobrescrevendo o estado vigente a cada coleta (RF-17). Sem historico de evolucao de etapas.
- Indices: UNIQUE em `nomus_id` (constraint de coluna); indice auxiliar `idx_nomus_processos_data_ultima_alteracao` para a janela incremental; indice opcional em `(empresa, tipo)` para filtros de UI.
- RLS ativa com a policy unica `is_conta_autorizada()` (USING + WITH CHECK), espelhando as demais tabelas do MVP (RNF-03).
- Triggers: `fn_audit_log()` (insert/update/delete em `audit_log`) e `fn_set_updated_at()` (BEFORE UPDATE), no padrao das tabelas auditadas.

#### D-02: Indice de chunks agnostico de origem - tabela generica coexistindo (RF-25, RNF-08/16, L-03)
- DECISAO: criar NOVA tabela `public.memoria_chunks` que COEXISTE com `aviso_chunks`. `aviso_chunks` permanece intacta (risco ZERO de regressao na busca de avisos em producao). Migracao dos chunks de avisos para a tabela unica fica para entrega posterior.
- Colunas de `memoria_chunks`:
  - `id uuid primary key default gen_random_uuid()`
  - `origem text not null` (ex.: `aviso`, `processo`)
  - `tipo text` (discriminador fino, ex.: `processo-venda-governamental`)
  - `registro_id uuid not null` (referencia generica ao registro de origem; ex.: `nomus_processos.id`; SEM FK rigida cross-tabela para permitir multiplas origens)
  - `ordem int`
  - `conteudo text not null`
  - `embedding vector(1024)` (mesma dimensao do provider bge-m3 do substrato)
- Indice vetorial HNSW `using hnsw (embedding vector_cosine_ops)`, identico ao de `aviso_chunks` (RNF-08).
- Indice auxiliar `(origem, registro_id)` para limpeza idempotente (apagar chunks antigos do registro antes de regravar) e para o filtro de escopo.
- RLS ativa com `is_conta_autorizada()` (RNF-03).
- Indexacao idempotente: ao reindexar um processo, apagar `memoria_chunks` onde `origem='processo' and registro_id=<id>` e regravar (RF-24). Reindexacao disparada quando o `conteudo_hash` muda (D-01 / RF-19).
- A RPC `busca_semantica_chunks` sera generalizada para ser origem-aware (union entre `aviso_chunks` e `memoria_chunks` ou consulta a `memoria_chunks` conforme o escopo), com a resposta ADITIVA: mantem o campo hoje retornado e adiciona `origem` + `registro_id` generico, preservando o contrato consumido pela Lia (RF-26, L-05). NAO se cria endpoint novo por recurso.

#### D-03: Generalizacao de `erros_ingestao` - aditivo (RF-34, RNF-05)
- ALTER TABLE aditivo, sem remover `aviso_id` (preserva os erros Effecti existentes e os componentes que ja leem `aviso_id`):
  - `add column origem text` (ex.: `effecti`, `nomus`)
  - `add column registro_id uuid` (referencia generica ao registro de origem; sem FK rigida)
  - `add column recurso text` (ex.: `processos`)
- Erro de processo Nomus grava `origem='nomus'`, `registro_id=nomus_processos.id`, `recurso='processos'`, com `aviso_id` nulo. Falha isolada de item vira registro aqui sem derrubar o lote (RNF-05).

#### D-04: Checkpoint e origem em `execucoes` - coluna `checkpoint jsonb` (RF-20/21/34)
- ALTER TABLE aditivo:
  - `add column checkpoint jsonb` (pagina/cursor atual, lote, contadores e estado de continuacao; formato evolui sem nova migracao; serve incremental e backfill futuro)
  - `add column fonte_id uuid references public.fontes(id)` (discrimina a origem Effecti x Nomus na observabilidade)
  - `add column recurso text` (ex.: `processos`)
- Indice auxiliar `idx_execucoes_fonte_id` para os filtros multi-origem das telas de observabilidade.
- A execucao so e marcada `concluida` apos todos os blocos da janela; falha de infra reflete `erro` preservando o `checkpoint` para diagnostico (RF-21). Realtime ja habilitado em `execucoes` permanece.

#### D-05: Recursos/tipos ativos em `config_ingestao` - coluna `recursos jsonb` (RF-06/07/08)
- ALTER TABLE aditivo: `add column recursos jsonb` guardando o mapa recurso->tipos[] (ex.: `{"processos":["Venda Governamental"]}`).
- As colunas existentes `modalidades`/`portais` (`text[] not null`, especificas do Effecti) permanecem; para a config Nomus ficam vazias (`'{}'`).
- A selecao persistida governa coleta manual e agendada, valendo na proxima execucao sem redeploy (RF-08). Estrutura comporta novos recursos/tipos futuros sem nova migracao.

#### D-06: Provisionamento da fonte `nomus` - seed idempotente (RF-00)
- Migracao/seed idempotente (padrao `where not exists`) cria uma linha em `public.fontes` com: `tipo='nomus'`, `nome` da fonte, `endpoint_base` da instancia Nomus, `estado_conexao='nao_configurada'`, `token_cifrado=null`, `ativa=true`, `ordem` (apos o Effecti no ciclo do orquestrador).
- Cria a `config_ingestao` vinculada (`fonte_id` da fonte Nomus) com `janela_dias=7` default, `modalidades='{}'`, `portais='{}'`, e `recursos` apenas com `processos`->`["Venda Governamental"]` ativo; suporte a data inicial especifica configuravel (nao apenas janela movel).
- A credencial e gravada apenas no Supabase Vault via RPC `set_fonte_secret`, com `fontes.token_cifrado` guardando so a referencia (RNF-01/02). Nunca em texto pleno, nunca retornada ao cliente, nunca em `audit_log` com valor.

#### Itens fora do escopo de Database nesta entrega
- Anexos (do Nomus e do Effecti) e suas tabelas/colunas.
- Campos personalizados do Nomus (write-only, indisponiveis por GET).
- Correlacao explicita processo <-> aviso (nenhuma FK ou tabela de vinculo agora).
- Migracao dos chunks de `aviso_chunks` para `memoria_chunks` (a unificacao fica para depois; nesta entrega as duas coexistem).
- Backfill historico (apenas o design de checkpoint o acomoda; janela inicial = 7 dias).

#### Dependencias de Database a confirmar na spec
- L-01: existencia de campo de ULTIMA ALTERACAO filtravel no GET de `/processos` define se `data_ultima_alteracao` alimenta a janela incremental ou se vale o fallback de re-scan dos nao finalizados.
- L-05: contrato de resposta exato consumido pela Lia em `v1-substrato-busca-semantica`, para garantir que a generalizacao da RPC seja aditiva.
- Nomes finais das colunas de datas/cliente de `nomus_processos` a ajustar conforme o payload real do GET.

### Backend

> Decisoes de Backend / servicos / API consolidadas e aprovadas pelo usuario (2026-06-06).
> Runtime existente: Supabase Edge Functions (Deno/TypeScript) em `supabase/functions/<nome>/index.ts`, padrao `Deno.serve(handler)`.
> Principio geral: reaproveitar o PADRAO existente (nao o codigo literal). Caminho do Effecti em producao permanece INTACTO (regressao zero, RNF-17). Toda generalizacao de endpoint/shared e ADITIVA. Nenhuma troca de framework.
> Convencoes preservadas em todo endpoint novo/alterado: `handleCorsPreflight` -> `assertMethod` -> AUTORIZACAO antes do corpo -> `parseJsonBody(req, zodSchema)` -> logica -> `jsonResponse`/`errorResponse({fn})`. Escrita no substrato sempre via `service_role` server-side. Segredos so como referencia no Vault. Auditoria via `logSensitiveAction` sem vazar segredo. Validacao server-side com zod em `_shared/validation.ts`.

#### B-01: Conector Nomus com contrato proprio (RF-10/11/12/13/14, RNF-06/17)
- DECISAO: novo `_shared/nomus-connector.ts` com `NomusConnector` que implementa o contrato `SourceConnector` (factory `createConnector("nomus", config)` passa a resolver o Nomus SEM alterar o caso `effecti`), porem com um TIPO DE SAIDA PROPRIO (`CollectedProcesso`) em vez de `CollectedAviso`. O caminho do Effecti fica 100% intacto (regressao zero).
- Reaproveita os UTILITARIOS de throttling ja existentes em `effecti-connector.ts` (`fetchWithBackoff`, `parseRetryAfter`, `computeBackoff`); se necessario, esses utilitarios sao extraidos para um shared comum SEM alterar o comportamento do Effecti.
- Autenticacao: header `Authorization: Basic <chave-integracao>` + `Content-Type: application/json`, com a chave lida do Vault em runtime (`getFonteSecret`), nunca de `.env`/cliente.
- Paginacao incremental `GET /rest/processos?pagina=N` a partir de 1, tratando a resposta como ARRAY. Filtro de `tipo` aplicado NO CLIENTE sobre o array (allowlist por recurso vinda de `config_ingestao.recursos`), descartando processos fora da allowlist antes de persistir (RF-09).
- Throttling: respeitar HTTP 429 aguardando `tempoAteLiberar`; processar em lotes com pausa entre lotes, com TAMANHO DE LOTE e PAUSA parametrizados em constantes configuraveis (defaults ~14 chamadas / ~5s); backoff exponencial com teto em 5xx; nao re-tentar 401.
- Multi-empresa por campo: a coleta usa UMA unica fonte+chave e grava o campo `empresa` de cada processo (famaha e darlu), sem multiplas instancias de fonte (RF-14).

#### B-02: Coleta em blocos com checkpoint dirigida pelo orquestrador (RF-20/21/29/30/31, RNF-04/06/10)
- DECISAO: a coleta Nomus roda em BLOCOS COM CHECKPOINT. O modulo de coleta processa K paginas por invocacao da Edge Function, persiste o `checkpoint` (pagina/cursor, lote, contadores, estado) em `execucoes` e RETORNA. A continuacao e retomada pelo `ingestao-orquestrar` (pg_cron existente) no proximo tick, ate `concluida`. SEM fila nova e SEM auto re-enfileiramento (fiel a decisao do discovery).
- AJUSTE no `ingestao-orquestrar` (`runCycle`): hoje faz `await runPipeline` de cada fonte ate concluir num unico ciclo. Passa a, por tick, ESCOLHER O PROXIMO RECURSO PENDENTE (por `fontes.ordem`/prioridade), rodar UM BLOCO (avancar o checkpoint) e retornar. Recursos cujo conector e por bloco (Nomus) avancam incrementalmente; o Effecti continua no seu fluxo atual sem regressao.
- Trava SINGLE-FLIGHT global preservada: no maximo uma execucao com `status='em_andamento'`; novo disparo (manual ou agendado) com execucao em andamento NAO inicia outra coleta (mesmo anti-sobreposicao ja usado em `ingestao-coletar`/`ingestao-orquestrar`).
- PRIORIDADE: incrementais de todas as fontes rodam primeiro (ordem cadastrada); o BACKFILL (faixa de menor prioridade) preenche janelas ociosas para evitar starvation. Ordem/prioridade configuravel.
- A execucao so e marcada `concluida` apos todos os blocos da janela; falha de infra reflete `erro` PRESERVANDO o `checkpoint` para diagnostico e retomada (RF-21). Realtime de `execucoes` continua refletindo o progresso ao vivo.

#### B-03: Pipeline proprio do Nomus - novo modulo `_shared/nomus-pipeline.ts` (RF-15/16/17/18/19/24, RNF-05/07/09)
- DECISAO: novo `_shared/nomus-pipeline.ts` com persistencia e indexacao proprias do recurso, deixando `runPipeline`/`persistAvisoBase`/`setStatusIndexacao` (caminho Effecti) INTACTOS. Compartilha apenas o provider de embeddings e os utils de throttling.
- Persistencia: upsert em `nomus_processos` com `onConflict: nomus_id`, sobrescrevendo o estado vigente a cada coleta (sem historico de etapas). Campos personalizados (write-only) e anexos NAO sao persistidos nesta entrega (RF-18).
- Reindexacao incremental: calcular HASH do conteudo textual canonico (campos textuais definidos, sobretudo `descricao`, concatenados deterministicamente) e gravar em `nomus_processos.conteudo_hash`. Disparar re-chunk/re-embed APENAS quando o hash mudar; controlar por `status_indexacao` (padrao dos avisos). Hash igual => zero reindexacao.
- Indexacao agnostica de origem: segmentar (`chunkText`) e gerar embeddings via provider plugavel existente (bge-m3, `vector(1024)`, HNSW `vector_cosine_ops`) gravando em `memoria_chunks` com `origem='processo'`/`tipo`/`registro_id=nomus_processos.id`. Idempotente: apagar os chunks antigos do registro (`origem` + `registro_id`) antes de regravar. NUNCA usar o modelo Claude na ingestao (RNF-09).
- Isolamento de falha por item (RNF-05): falha de tratamento/indexacao de um processo NAO derruba o lote; vira registro em `erros_ingestao` usando a referencia generica de origem (`origem='nomus'`, `registro_id=nomus_processos.id`, `recurso='processos'`, severidade/etapa), e a coleta continua. `aviso_id` fica nulo nesses erros.
- Dedup estavel: reingestao do mesmo `nomus_id` produz exatamente uma linha com o estado vigente; `empresa` apenas discrimina origem e nao compoe a chave (RNF-07).

#### B-04: Generalizacao dos endpoints de fonte e coleta - aditiva (RF-00..RF-09, RF-28/29, RNF-01/02/12)
- `_shared/validation.ts`: generalizar o `coletarSchema` para aceitar `fonte` (`effecti` | `nomus`) e `recurso` (ex.: `processos`), substituindo o `z.literal("effecti")` por enum, mantendo `janelaDias`/`gatilho` e a compatibilidade do disparo Effecti atual. Novos schemas: credencial generica por fonte (token nao-vazio) e config de recursos/tipos ativos (mapa recurso->tipos[]).
- `ingestao-coletar`: parametrizar `fonte`+`recurso` no handler; trocar `getFonteByTipo()` (hoje sem argumento, resolve Effecti) por `getFonteByTipo(fonte)`; selecionar o pipeline conforme o recurso (Effecti -> `runPipeline`; Nomus -> bloco de `nomus-pipeline`). Mantem retorno imediato `202 { execucaoId, status:'em_andamento' }` e a trava anti-duplo-disparo (RF-28/29).
- `fontes-credencial`: gravar a chave Nomus no Vault via `set_fonte_secret`, persistindo apenas a referencia em `fontes.token_cifrado`; validar corpo com zod e exigir `requireAuthorizedUser` ANTES do corpo; resolver a fonte pelo `tipo` informado (sem path fixo `effecti`), preservando o Effecti (RF-01/02/05, RNF-01).
- `fontes-testar`: ler a chave do Vault em runtime e fazer uma requisicao leve a `GET /rest/processos`, atualizando `fontes.estado_conexao` (`conectada`/`erro`/`nao_configurada`); classificar a falha em `unauthorized` (401), `rate_limited` (429), `timeout` e `unknown`, com mensagem distinta por causa (copy especifica por fonte) e latencia em ms, no padrao de `ConnectionTestError` (RF-03/04, RNF-12).
- Provisionamento (RF-00) e configuracao de recursos/tipos (RF-06/07/08) consomem o seed e a coluna `config_ingestao.recursos` ja definidos na secao Database; a selecao governa coleta manual e agendada, valendo na proxima execucao sem redeploy.
- Seguranca: a chave de integracao do Nomus NUNCA em texto pleno, nunca em `.env` de producao, nunca retornada ao cliente, nunca em `audit_log` com valor (apenas referencia/fonte). Defesa em profundidade: autorizacao na borda (`requireAuthorizedUser`) + RLS; escrita via `service_role` server-side (RNF-01/02).

#### B-05: Busca semantica multi-origem - generalizacao aditiva do endpoint /v1 (RF-25/26/27, RNF-08/16, L-05)
- DECISAO: generalizar `v1-substrato-busca-semantica` SEM novo endpoint por recurso e SEM breaking para a Lia.
- `buscaSemanticaSchema`: adicionar parametro `escopo` OPCIONAL (ex.: `tudo` | `processos` | `avisos` | por `tipo`), default `tudo`; manter `query`/`topK` e o clamp existente.
- RPC `busca_semantica_chunks` passa a ser ORIGEM-AWARE (union de `aviso_chunks` + `memoria_chunks` conforme o escopo), preservando o uso do indice HNSW `vector_cosine_ops` sobre `vector(1024)` (RNF-08). O filtro de escopo nao deve impedir o uso do indice vetorial.
- RESPOSTA ADITIVA: manter `id`/`score`/`verbatim` exatamente como hoje e ADICIONAR `origem` + `registro_id` genericos. Zero breaking no contrato consumido pela Lia (confirmar campos exatos na spec, L-05). Autenticacao `/v1` permanece via `authenticateV1` (API key da Lia no Vault OU sessao do cockpit) e a RPC continua `SECURITY DEFINER` executavel so por `service_role`.
- NAO criar vinculo explicito processo <-> aviso: a busca unificada e independente de correlacao entre registros (RF-27). Novos recursos futuros entram apenas passando a ser indexados em `memoria_chunks`, sem nova RPC nem novo endpoint (RNF-16).

#### Itens fora do escopo de Backend nesta entrega
- Escrita de volta no Nomus (POST/PUT de processos/avisos): fica para fase posterior (automacao de analise de editais).
- Download/parse de anexos (Nomus e Effecti): o pipeline Nomus NAO baixa nem extrai arquivos nesta entrega.
- Generalizacao total de `runPipeline`/`SourceConnector` para um contrato unico: NAO sera feita agora (Nomus usa contrato e pipeline proprios para garantir regressao zero no Effecti).
- Backfill historico: apenas o desenho de blocos+checkpoint o acomoda; a janela inicial e de 7 dias.
- Migracao dos chunks de `aviso_chunks` para `memoria_chunks`: nesta entrega as duas tabelas coexistem.

#### Dependencias de Backend a confirmar na spec
- L-01: presenca de campo de ULTIMA ALTERACAO filtravel via `?query=campoData>...` no GET de `/processos`. Se sim, o conector usa a janela movel por alteracao; se nao, aplica o fallback de re-scan dos processos nao finalizados (etapa nao terminal). O `nomus-connector` deve suportar AMBOS os modos.
- L-02: detalhes operacionais da referencia externa "aiox" (auth, endpoints, tratamento de campos) a serem fornecidos pelo usuario.
- L-05: contrato de resposta exato consumido hoje pela Lia em `v1-substrato-busca-semantica`, para garantir que a generalizacao multi-origem seja estritamente aditiva.
- Tamanho de bloco (paginas por invocacao) e parametros de lote/pausa a calibrar na spec conforme o rate limit observado do Nomus.

### Frontend

> Decisoes de Frontend/UI/UX consolidadas e aprovadas pelo usuario (2026-06-06).
> Stack existente: Next.js 15.5 (App Router) + React 19 + TypeScript; grupo de rotas autenticado `(cockpit)`. Dados via TanStack Query 5 (`src/hooks`: use-admin, use-monitoring, use-execucoes-realtime). Formularios com react-hook-form + zod. Realtime do Supabase na tela de Execucoes.
> Estilizacao: Tailwind 3.4 + shadcn/ui (new-york) com design system de CLASSES SEMANTICAS em `globals.css` sobre tokens CSS (`var(--accent)`, `var(--surface)`, etc.): `.screen`, `.page-head`, `.card`, `.btn`/`.btn-primary`/`.btn-sm`, `.field`, `.chk-grid`/`.chk on`/`.chk disabled`, `.section-title`, `.kv`, `.save-note`, `.mono`, `.tnum`, status `pill`, `.tbl-wrap`. NENHUMA troca de framework ou de sistema de estilos. NENHUM padrao visual novo: tudo reusa as classes/tokens existentes.
> Principio geral: ADITIVO e regressao zero no caminho Effecti, espelhando a filosofia ja adotada em Database e Backend desta feature. O Effecti em producao permanece intacto. O Design Lock de navegacao (`src/lib/nav.ts`, 5 itens) NAO e alterado: o Nomus NAO vira item de menu novo; vive dentro de `/fontes`, `/execucoes` e `/erros`.

#### F-01: Bloco ativo da fonte Nomus em /fontes - componentes dedicados (RF-32, US-18)
- DECISAO: componentes DEDICADOS Nomus, espelhando o padrao Effecti, para manter o Effecti 100% intacto (regressao zero), coerente com a filosofia aditiva do backend/DB.
- Novos componentes (mesma estrutura visual e de estados do Effecti):
  - `src/components/cockpit/fonte-nomus-block.tsx` (analogo a `fonte-effecti-block.tsx`): compoe credencial + painel de configuracao da mesma fonte, com o painel revelado pelo botao "Configurar" (estado compartilhado, scroll suave ate o painel), no mesmo padrao do bloco Effecti.
  - `src/components/cockpit/cred-form-nomus.tsx` (analogo a `cred-form.tsx`): cadastro/teste da chave de integracao Nomus, estados idle/loading/success/error, acoes independentes "Salvar credencial" e "Testar conexao", token mascarado/cifrado (nunca exibido apos salvo, RNF-01), `StatusPill` de conexao via `conexaoDescriptor` e copy de erro especifica por causa (401/429/timeout/unknown).
  - `src/components/cockpit/cfg-form-nomus.tsx` (analogo a `cfg-form.tsx`): janela de dias (default 7) + selecao de recursos/tipos (ver F-02) + botao "Executar coleta agora" com a mesma trava de coleta em andamento (single-flight) e o aviso de alteracoes nao salvas.
- A tela `fontes/page.tsx` (Server Component) hidrata os snapshots da fonte Nomus via Supabase server client (RLS), no mesmo padrao das funcoes `loadFonte`/`loadConfig` do Effecti, e renderiza o `FonteNomusBlock` ao lado/abaixo do bloco Effecti. O segredo NUNCA trafega ao cliente: `configurado` deriva so da presenca da referencia no Vault (`token_cifrado != null`).
- O item "ERP Nomus" hoje listado em "Fontes futuras" (desabilitado, em `fonte-effecti-block.tsx`) SAI dessa lista de desabilitados e passa a ser o bloco ativo. As demais fontes futuras (E-mail operacional, Google Drive) permanecem desabilitadas.

#### F-02: UI de recursos e tipos ativos - chk-grid com tipos aninhados (RF-32, US-18; consome D-05/config_ingestao.recursos)
- DECISAO: reusar o padrao `chk-grid`/`.chk` ja usado para modalidades/portais no `cfg-form`. Recursos como grupo de toggles; sob o recurso ativo (`processos`) os tipos aparecem como sub-itens selecionaveis (`Venda Governamental` marcado). Sem introduzir accordion ou qualquer padrao visual novo.
- Nesta entrega vem ATIVO apenas o recurso `processos` + tipo `Venda Governamental`. Os demais recursos (propostas, pedidos, NFes, contas a receber) aparecem VISIVEIS e DESLIGADOS usando `.chk disabled` (cinza), exatamente como o bloco "Fontes futuras" ja faz hoje.
- A selecao e persistida no mapa `recursos` (`{"processos":["Venda Governamental"]}`) de `config_ingestao` (coluna definida em D-05) e governa coleta manual e agendada, valendo na proxima execucao sem redeploy. A estrutura comporta novos recursos/tipos no futuro sem nova tela.
- Validacao cliente com zod espelhando o schema do backend (janela inteira em faixa valida; recurso ativo com ao menos um tipo), defesa em profundidade antes do submit.

#### F-03: Execucoes e Erros multi-origem - filtro e coluna de Origem (RF-34, US-19)
- DECISAO: adicionar coluna e filtro de ORIGEM (Todas / Effecti / Nomus) e de RECURSO/tipo nas telas de Execucoes e Erros, reusando o padrao de filtro por botoes (`btn`/`btn-sm` + `aria-pressed`) ja presente em `erros-client.tsx`, e a estrutura `COLUMNS` por `variant` de `runs-table.tsx`/`erros-table.tsx` (adicao ADITIVA de coluna).
- `runs-table.tsx`: incluir coluna "Origem" (e "Recurso" quando aplicavel) de forma aditiva nos `COLUMNS` da variant `execucoes`, lendo `origem`/`recurso` do registro. O Dashboard (variant `dashboard`) NAO e alterado (tela travada).
- `erros-table.tsx`: generalizar a coluna hoje rotulada "Aviso / item" para "Registro / item", exibindo `registro_id` + origem. O Effecti continua mostrando `avisoId`.
- ACAO "Investigar" de erro: continua aparecendo SO para avisos Effecti (que possuem destino `/edital/[avisoId]`). Para erros de origem Nomus o link fica OCULTO, exatamente como ja ocorre hoje quando `avisoId` e nulo. NAO sera criada tela de detalhe de processo Nomus nesta entrega (fora de escopo, sem US; nenhuma rota/itens de menu novos, Design Lock preservado).
- Filtros consomem os campos `origem`/`recurso` adicionados (aditivos) a `execucoes` e `erros_ingestao` (D-03/D-04). Realtime de Execucoes preservado.

#### F-04: Estado/saude da fonte Nomus apenas no bloco /fontes (RF-35, US-19)
- DECISAO: a saude/estado da fonte Nomus (ultima coleta, sucesso/falha, ultima verificacao) e exposta pelo `StatusPill` + dados do bloco em `/fontes` (espelhando o `cred-form` do Effecti), em paridade com o Effecti. Execucoes e Erros cobrem o detalhe via filtro por origem (F-03).
- O Dashboard NAO e alterado nesta entrega (tela travada pelo Design Lock); evita ampliar o destrave necessario. Card de saude por fonte no Dashboard fica como melhoria futura, se desejado.

#### F-05: Tipos e hooks de dados - generalizacao aditiva (RF-32/34, US-18/19)
- `src/lib/api/types.ts`: generalizar `FonteEffectiState` para um `FonteState` generico (ou adicionar `FonteNomusState` analogo), mantendo compatibilidade; `ConfigIngestaoState` ganha o mapa `recursos` (recurso->tipos[]); `Execucao` e `Erro` ganham campos `origem` e `recurso` (aditivos, opcionais para nao quebrar o consumo Effecti); `Erro` passa a expor `registroId`/`origem` alem do `avisoId` legado.
- `src/hooks/use-admin.ts` e `src/hooks/use-monitoring.ts`: parametrizar credencial, teste, config e coleta por `fonte`/`recurso`, mantendo o disparo Effecti atual compativel (defaults preservam o comportamento existente).

#### F-06: Design Lock - revisao aditiva sem novo item de menu (RF-33, US-18)
- DECISAO: as novas superficies (bloco Nomus em `/fontes`; colunas/filtros de origem em `/execucoes` e `/erros`) entram por REVISAO ADITIVA do Design Lock (`manifest.json locked:true`), sem quebrar telas travadas.
- `src/lib/nav.ts` permanece INTACTO: continua com 5 itens em 2 grupos; o Nomus NAO recebe item de menu proprio. Nenhuma rota nova (sem `/processo/[id]`).

#### Itens fora do escopo de Frontend nesta entrega
- Tela de detalhe de processo Nomus (`/processo/[id]`) e qualquer novo item de menu.
- Alteracoes no Dashboard (card de saude por fonte fica como melhoria futura).
- UI de correlacao processo <-> aviso (correlacao adiada; sem vinculo na interface).
- UI de anexos (Nomus/Effecti) e de campos personalizados (write-only, indisponiveis por GET).
- Escrita de volta no Nomus (cadastro/edicao de processos pela UI): fase posterior.

#### Dependencias de Frontend a confirmar na spec
- Nomes finais dos campos `origem`/`recurso` no contrato camelCase das Edge Functions (alinhar `types.ts` ao backend) apos D-03/D-04.
- Copy/labels finais dos recursos futuros exibidos desabilitados (propostas, pedidos, NFes, contas a receber) conforme nomenclatura do Nomus.
- Confirmacao de que o teste de conexao Nomus reaproveita o contrato `TestarConexaoResponse` (causa/latencia) sem alteracao de shape.
