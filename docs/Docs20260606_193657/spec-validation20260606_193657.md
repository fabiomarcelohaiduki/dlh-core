# Validation Report

## Status: PASS

## Completude (Discovery → SPEC)
Todos os itens do discovery estao cobertos na SPEC.

> Notas de verificacao (nao sao issues):
> - US-00..US-19 mapeadas para artefatos concretos: seed (2.4), endpoints (3.2), tabelas (2.1), componentes (4.2) e hooks (4.3).
> - Stack (TS/Next 15.5/Supabase Edge/PostgreSQL+pgvector), plataforma (web + API `/v1`), auth (Google OAuth, perfil `interno`) e embeddings (bge-m3, `vector(1024)`, HNSW) refletidos.
> - Conector Nomus, throttling (429/`tempoAteLiberar`/backoff), coleta em blocos com checkpoint, dedup por `nomus_id`, indexacao agnostica de origem e busca multi-origem com escopo: todos presentes.
> - RNFs e SEC-01..SEC-10 refletidos no checklist de seguranca (5.2). Upload/webhook corretamente marcados N/A (fora de escopo no PRD).
> - Divergencias de design (generalizar `aviso_chunks` em vez de criar `memoria_chunks`; nomes de colunas de data; `registro_id` como `nomus_id` em `erros_ingestao`) sao explicitamente autorizadas pelas lacunas L-03/L-05 e notas "nomes ajustaveis na spec" do PRD; satisfazem RF-25/RF-26 e nao foram contabilizadas como issue.

## Consistencia Backend ↔ Frontend
Backend e frontend estao consistentes.

> Notas de verificacao (nao sao issues):
> - Cada endpoint de 3.2 tem hook em 4.3 com return type batendo no response body: `fontes-credencial` -> `useSalvarCredencial`; `fontes-testar` -> `useTestarConexao` (`estado_conexao/causa/mensagem/latencia_ms`); `ingestao-config` -> `useIngestaoConfig`; `ingestao-coletar` -> `useColetar` (`execucao_id/estado/ja_em_andamento`); `v1-substrato-busca-semantica` -> `useBuscaSemantica`.
> - `ingestao-orquestrar` e endpoint interno (pg_cron/service_role), sem hook por design - esperado.
> - Rotas `/fontes`,`/execucoes`,`/erros` marcadas protected; backend exige `requireAuthorizedUser`.
> - 401 -> invalida sessao/redireciona `/login`; 409 single-flight tratado em 4.6; progresso via Realtime de `execucoes` (nao polling).

## Consistencia Database ↔ Backend
Database e backend estao consistentes.

> Notas de verificacao (nao sao issues):
> - Toda tabela tem acesso por endpoint/pipeline: `fontes`, `config_ingestao`, `nomus_processos` (upsert no pipeline), `aviso_chunks` (RPC `busca_semantica_chunks`), `execucoes`, `erros_ingestao`, `audit_log`, `config_agendamento`.
> - Campos retornados existem nas tabelas: `useExecucoes`/`useErros`/`useFontes` mapeiam apenas colunas declaradas em 2.1; busca retorna `aviso_id/registro_id/origem/verbatim` (existentes) + `similaridade` (computada).
> - FKs coerentes: `execucoes.fonte_id -> fontes(id)`, `erros_ingestao.execucao_id -> execucoes(id)`.
> - RLS `is_conta_autorizada()` em todas as tabelas; escrita da coleta via `service_role` server-side.
> - Seed idempotente da fonte `nomus` + `config_ingestao` presente (resolve `getFonteByTipo("nomus")`).
> - Indexes cobrem filtros/janela: `data_alteracao`, `empresa`, `tipo`, `status_indexacao` em `nomus_processos`; `fonte_id/recurso/estado` em `execucoes`.

## Resumo
- Total de issues: 0
