# Relatório de Validação da SPEC — DLH Core (MVP / Fase 1)

> **Gerado em:** 2026-06-06
> **SPEC validada:** `C:\Users\Dell\dlh-core\SPEC.md`
> **PRD:** `docs\Docs20260605_192252\PRD20260605_192252.md`
> **User Stories:** `docs\Docs20260605_192252\stories-requisitos20260605_192252.md`
> **Design Lock:** `docs\Docs20260605_192252\design\design-contract.json` (`locked: true`, lockedAt 2026-06-06T03:08:57Z)
> **Marcadores:** `[MISS]` = item ausente · `[CONFLICT]` = item contraditório

## Status: PASS

Nenhuma issue bloqueante encontrada. A SPEC cobre integralmente o Design Lock, as user stories e os requisitos do PRD, sem introduzir telas, rotas, componentes ou estados fora do contrato travado.

---

## 1. Completude (Design → SPEC)

### 1.1 Telas do design presentes na SPEC
Todas as 8 telas do `design-contract.json` aparecem na SPEC (§4.2 Mapa de Rotas e Telas).

| Tela (design id) | Rota design | Presente na SPEC | Rota real (App Router) |
|---|---|---|---|
| `login` | `#login` | OK | `/login` (fora do shell) |
| `dashboard` | `#dashboard` | OK | `/dashboard` (home; `/` redireciona) |
| `execucoes` | `#execucoes` | OK | `/execucoes` |
| `erros` | `#erros` | OK | `/erros` |
| `edital` | `#edital` | OK | `/edital/[avisoId]` (sem item de menu) |
| `fontes` | `#fontes` | OK | `/fontes` |
| `ingestao` | `#ingestao` | OK | `/ingestao` |
| `api` | `#api` | OK | `/api` (console de validação — delta-002) |

Resultado: **0 telas ausentes.**

### 1.2 Rotas da SPEC existentes no design
Todas as rotas declaradas na SPEC correspondem a telas do design. A SPEC documenta explicitamente a tradução de hash routing (`#dashboard`) para rotas App Router, preservando direção visual, estados e navegação travados (§4.1 / §4.2). Navegação primária (6 itens) + secundária (`edital`, destino sem item de menu) idênticas ao contrato.

Resultado: **0 rotas novas fora do design.**

### 1.3 apiExpectations do design com endpoint no backend
As 11 `apiExpectations` do design estão resolvidas no backend (SPEC §3.1 estrutura de pastas e §3.2 tabela de endpoints).

| apiExpectation | Operação | Endpoint na SPEC |
|---|---|---|
| `api-auth-google` | POST /auth/google | OK (`auth-google`) |
| `api-healthcheck` | GET /ingestao/healthcheck | OK (`ingestao-healthcheck`) |
| `api-coleta-demanda` | POST /ingestao/coletar | OK (`ingestao-coletar`) |
| `api-listar-execucoes` | GET /ingestao/execucoes | OK (`ingestao-execucoes`) |
| `api-listar-erros` | GET /ingestao/erros | OK (`ingestao-erros`) |
| `api-detalhe-edital` | GET /substrato/avisos/:id | OK (`substrato-aviso`) |
| `api-reprocessar` | POST /substrato/avisos/:id/reindexar | OK (`substrato-reindexar`) |
| `api-salvar-credencial` | PUT /fontes/effecti/credencial | OK (`fontes-credencial`) |
| `api-testar-conexao` | POST /fontes/effecti/testar | OK (`fontes-testar`) |
| `api-salvar-config` | PUT /ingestao/config | OK (`ingestao-config`) |
| `api-busca-semantica` | POST /substrato/busca-semantica (`/v1`) | OK (`substrato-busca`) |

Resultado: **0 endpoints esperados pelo design sem backend.**

### 1.4 dataRequirements com tabela/campo no database
As 8 `dataRequirements` do design têm origem no schema (SPEC §2 e mapeamento §4.6).

| dataRequirement | Origem no banco (SPEC) | Campos cobertos |
|---|---|---|
| `data-sessao` | Supabase Auth + `contas_autorizadas` | email, provider, token |
| `data-healthcheck` | view `vw_healthcheck` | statusIngestao, ultimaSync, totalAvisos, itensComErro |
| `data-execucao` | tabela `execucoes` | id, inicio, gatilho, janela_dias, novos, alterados, duracao, status |
| `data-erro` | tabela `erros_ingestao` | id, severidade, etapa, aviso_id, mensagem, quando |
| `data-edital` | `avisos` (+ `aviso_arquivos`, `aviso_chunks`) | id, modalidade, orgao, objeto, conteudo_verbatim, payload_bruto, chunks/embeddingsOk (derivados) |
| `data-fonte` | tabela `fontes` (token via Vault) | nome, tipo, endpoint_base, estado_conexao, token_cifrado |
| `data-config-ingestao` | tabela `config_ingestao` | frequencia, horario_referencia, janela_dias, modalidades, portais |
| `data-busca-semantica` | `aviso_chunks.embedding` (HNSW) → `avisos.conteudo_verbatim` | query, results, score |

Resultado: **0 dataRequirements sem tabela/campo.**

---

## 2. Rastreabilidade (Story → Tela/Fluxo)

Todas as 16 user stories cobertas pelo escopo têm tela ou fluxo correspondente (SPEC §4.7).

| User story | Tela(s)/Fluxo na SPEC | Status |
|---|---|---|
| US-00 (estrutura/navegação) | login, dashboard, shell/sidebar; onboarding via estados empty/idle | OK |
| US-21 (autenticação) | login (+ allowlist/logout no shell) | OK |
| US-02 (coleta automática) | background, refletido em dashboard/execucoes/fontes | OK |
| US-03 (agendamento/janela) | ingestao | OK |
| US-04 (coleta sob demanda) | dashboard, execucoes, ingestao | OK |
| US-05 (sync incremental) | dashboard, execucoes (contagem novos/alterados) | OK |
| US-07 (fontes/credenciais) | fontes | OK |
| US-08 (indexação/verbatim) | edital, api (pipeline + embeddings) | OK |
| US-10 (persistência/MOE) | edital (payload bruto integral) | OK |
| US-14 (detalhe do edital) | edital (acesso só via erro) | OK |
| US-15 (status/healthcheck) | dashboard, execucoes | OK |
| US-16 (erros) | erros, dashboard, edital (reprocessar) | OK |
| US-17 (consumo Lia API) | api (console de validação — delta-002) | OK |
| US-18 (busca semântica) | api (playground) | OK |
| US-19 (arquivos de edital) | edital (background) | OK |
| US-20 (modalidades/portais) | ingestao | OK |

Stories de background sem UI dedicada (US-02, US-05, US-08, US-10, US-19) são representadas como estados/dados nas telas existentes, conforme delta-003 — abordagem documentada e consistente com o Design Lock.

Resultado: **0 user stories sem tela ou fluxo aplicável.**

---

## 3. Consistência Interna (Componentes/Estados do Design → SPEC)

### 3.1 Componentes
Todos os 10 componentes do design estão na SPEC (§4.3), com os estados travados preservados.

| Componente (design id) | Estados (design) | Estados na SPEC | Status |
|---|---|---|---|
| `cmp-gbtn` | idle, loading, error | idle, loading, error | OK |
| `cmp-sidebar` | active | active | OK |
| `cmp-stat-card` | — | — | OK |
| `cmp-runs-table` | processing, empty | processing, empty | OK |
| `cmp-erros-table` | empty | empty | OK |
| `cmp-status-pill` | ok, run, warn, err, idle | ok, run, warn, err, idle | OK |
| `cmp-pipeline` | done, error, skip | done, error, skip | OK |
| `cmp-cred-form` | idle, loading, success, error | idle, loading, success, error | OK |
| `cmp-cfg-form` | idle, success, error | idle, success, error | OK |
| `cmp-search` | idle, loading, success, empty | idle, loading, success, empty | OK |

### 3.2 Estados de tela
Os estados por tela (§4.5) coincidem exatamente com os `states` do `design-contract.json`.

| Tela | Estados (design) | Estados (SPEC §4.5) | Status |
|---|---|---|---|
| login | idle, loading, error, success | idle, loading, error, success | OK |
| dashboard | loading, success, processing, error, empty | loading, success, processing, error, empty | OK |
| execucoes | loading, success, processing, empty | loading, success, processing, empty | OK |
| erros | loading, success, empty | loading, success, empty | OK |
| edital | loading, success, error | loading, success, error | OK |
| fontes | idle, loading, success, error | idle, loading, success, error | OK |
| ingestao | idle, success, error, processing | idle, success, error, processing | OK |
| api | idle, loading, success, empty | idle, loading, success, empty | OK |

### 3.3 Tokens visuais
Os tokens de cor, tipografia, espaçamento e raios da SPEC (§4.4) reproduzem fielmente o bloco `visual.tokens` do design. Convenção de status (ok/run/warn/err/idle) consistente entre pill e pipeline.

Resultado: **0 componentes ou estados relevantes omitidos.**

---

## 4. Observações não-bloqueantes

> Itens abaixo NÃO falham a validação; registrados para higiene documental.

- **Referência a `delta-006`:** a SPEC (§4.5, sobre o onboarding) cita `delta-006/conflito resolvido 6`, mas o `design-contract.json` define apenas `delta-001`…`delta-005`. A decisão de onboarding sem tela nova é coerente com o Design Lock (orquestra estados `empty`/`idle` existentes), então não há contradição funcional — apenas um identificador de delta inexistente no contrato. Sugestão: alinhar a numeração do delta ou removê-la.
- **Rota `/api` (App Router):** o nome `/api` coincide com a convenção de rotas de API do Next.js; é fiel ao design (`#api`) e descrito como console de validação humana (delta-002). Sem impacto na validação; apenas atenção na fase técnica para evitar colisão de namespace.

---

## 5. Conclusão

| Critério de falha | Resultado |
|---|---|
| Tela do design ausente na SPEC | Nenhuma |
| Rota nova na SPEC fora do design | Nenhuma |
| Endpoint esperado pelo design sem backend | Nenhum |
| dataRequirement sem tabela/campo | Nenhum |
| User story sem tela ou fluxo (quando aplicável) | Nenhuma |
| Componente/estado relevante do design omitido | Nenhum |

A SPEC está **consistente, completa e rastreável** em relação ao PRD, às user stories e ao Design Lock. Validação aprovada (**PASS**).
