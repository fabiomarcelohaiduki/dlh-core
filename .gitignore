# dependencies
/node_modules
.github/scripts/node_modules/
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# certificados digitais (nunca commitar)
*.pfx
*.p12
docs/CP/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env*.local

# pasta de referencia com secrets (sera excluida)
docs/pasta sera excluida/
"docs/pasta sera excluida/"

# typescript
*.tsbuildinfo

# vercel
.vercel

# supabase (estado local do CLI)
supabase/.temp/

# WIP local que nunca entra no repo (specs, docs, scripts de apoio, artefatos de
# pipeline e ferramentas). Tudo fica no disco, so nao e versionado. Os arquivos
# ja rastreados foram destrackeados via `git rm --cached` no mesmo commit.
/SPEC.md
/SPEC_PROGRESS.md
/docs/
/scripts/
/skill/
/.lionclaw/
/.codegraph/
/.dlh-dryrun/
/discovery-notes.md
/.sprint-validation-report.md

# Teste orfao: escrito contra um refactor de ingestao-orquestrar (OrquestradorDeps,
# tickFonte) que ainda nao existe no index.ts -> nao type-checka. Fica no disco ate
# o refactor acontecer; remover esta linha quando for committar junto.
supabase/functions/ingestao-orquestrar/index_test.ts
