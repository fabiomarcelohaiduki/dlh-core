project_id = "qvggrrirsjidtqsdvmxf"

# Chamada pelo pg_cron com X-Cron-Secret puro (SEM header Authorization). Se o
# verify_jwt ficar no default (ON), o GATEWAY do Supabase barra com 401
# {UNAUTHORIZED_NO_AUTH_HEADER} antes do codigo rodar -> ciclo agendado morto
# em silencio. A funcao se autentica sozinha (assertInternalAuth: X-Cron-Secret
# do Vault OU service_role bearer), entao desligar a checagem do gateway aqui e
# seguro. Sem este pin, todo `supabase functions deploy` sem --no-verify-jwt
# reabre o bug.
[functions.ingestao-orquestrar]
verify_jwt = false

# O callback do OAuth do Drive chega SEM Authorization (o navegador vem do
# Google), e o runner chama 'access-token' so com X-Cron-Secret. Com verify_jwt
# no default (ON) o gateway barraria ambos antes do codigo. A funcao se
# autentica sozinha (sessao / state CSRF / X-Cron-Secret), entao desligar a
# checagem do gateway aqui e seguro. Sem este pin, deploy sem --no-verify-jwt
# reabre o bug.
[functions.drive-oauth]
verify_jwt = false

# Mesma razao do drive-oauth: o callback do OAuth do Gmail chega SEM
# Authorization (navegador vindo do Google) e o runner chama 'access-token' so
# com X-Cron-Secret. A funcao se autentica sozinha (sessao / state CSRF /
# X-Cron-Secret). Sem este pin, deploy sem --no-verify-jwt reabre o bug 401.
[functions.gmail-oauth]
verify_jwt = false

# Backfill de indexacao auto-encadeado: reenfileirar_indexacao() dispara este
# Edge via pg_net com X-Cron-Secret PURO (sem Authorization). Com verify_jwt no
# default (ON) o gateway barraria a cadeia com 401 antes do codigo -> backfill
# morto em silencio. A funcao se autentica sozinha (assertInternalAuth:
# X-Cron-Secret do Vault OU service_role bearer). Sem este pin, deploy sem
# --no-verify-jwt reabre o bug.
[functions.documentos-indexar]
verify_jwt = false

# Busca semantica do acervo /v1: autentica via authenticateV1 (API key da Lia
# lia_sk_ no Vault OU sessao do cockpit). A API key NAO e um JWT, entao com
# verify_jwt no default (ON) o gateway barra o Bearer lia_sk_ com
# {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo. A funcao se autentica
# sozinha (compara em tempo constante / recai p/ sessao humana) e devolve 401
# quando falta credencial. Sem este pin, deploy sem --no-verify-jwt reabre o
# bug e a Lia nao consegue chamar a tool.
[functions.v1-acervo-busca-semantica]
verify_jwt = false

# Busca semantica no CATALOGO de produtos /v1 (tool produtos_busca da Lia na
# triagem): mesma autenticacao da busca do acervo (API key da Lia lia_sk_ no
# Vault OU sessao do cockpit via authenticateV1). A API key NAO e JWT -> sem
# este pin o gateway barra o Bearer lia_sk_ com {UNAUTHORIZED_INVALID_JWT_FORMAT}
# antes do codigo. Sem o pin, deploy sem --no-verify-jwt reabre o bug e a Lia
# nao consegue cruzar item de edital -> catalogo na triagem.
[functions.v1-produtos-busca-semantica]
verify_jwt = false

# Leitura integral de documento do acervo /v1: mesma autenticacao da busca
# (API key da Lia lia_sk_ no Vault OU sessao do cockpit via authenticateV1).
# A API key NAO e JWT -> sem este pin o gateway barra o Bearer lia_sk_ com
# {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo. Sem o pin, deploy sem
# --no-verify-jwt reabre o bug e a Lia nao consegue chamar a tool.
[functions.v1-acervo-ler-documento]
verify_jwt = false

# SQL read-only no substrato tabular /v1 (tool #4 do RAG): mesma autenticacao
# das outras tools (API key da Lia lia_sk_ no Vault OU sessao do cockpit via
# authenticateV1). A API key NAO e JWT -> sem este pin o gateway barra o Bearer
# lia_sk_ com {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo. Sem o pin,
# deploy sem --no-verify-jwt reabre o bug e a Lia nao consegue chamar a tool.
[functions.v1-substrato-sql]
verify_jwt = false

# Backfill da indexacao dos PROCESSOS (perna processos do RAG). Mesma
# autenticacao interna do documentos-indexar (X-Cron-Secret do Vault OU
# service_role bearer; o pg_net/cron manda so X-Cron-Secret, sem Authorization).
# Sem este pin, deploy sem --no-verify-jwt reabre o bug do gateway (401 antes
# do codigo) e a cadeia agendada morre em silencio.
[functions.processos-indexar]
verify_jwt = false

# Busca semantica dos PROCESSOS /v1: mesma autenticacao das outras tools (API
# key da Lia lia_sk_ no Vault OU sessao do cockpit via authenticateV1). A API
# key NAO e JWT -> sem este pin o gateway barra o Bearer lia_sk_ com
# {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo. Sem o pin, deploy sem
# --no-verify-jwt reabre o bug e a Lia nao consegue chamar a tool.
[functions.v1-acervo-busca-processos]
verify_jwt = false

# Tools de Relacionamentos /v1 (relacionamentos_no, relacionamentos_vizinhos
# e relacionamentos_buscar da Lia): autenticam via authenticateV1 com API key
# read-only da Lia. A API key NAO e JWT, entao o gateway precisa deixar a
# borda da funcao autenticar.
[functions.v1-relacionamentos-no]
verify_jwt = false

[functions.v1-relacionamentos-vizinhos]
verify_jwt = false

[functions.v1-relacionamentos-buscar]
verify_jwt = false

# Fila de triagem da esteira /v1 (modo lion): a Lia puxa o trabalho pendente
# via authenticateV1 (API key lia_sk_ no Vault, escopo read-only:busca-semantica
# OU sessao do cockpit). A API key NAO e JWT -> sem este pin o gateway barra o
# Bearer lia_sk_ com {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo. Sem o
# pin, deploy sem --no-verify-jwt reabre o bug e a esteira nunca recebe trabalho.
[functions.v1-triagem-fila]
verify_jwt = false

# Veredito da triagem da esteira /v1 (modo lion): a Lia posta a confianca crua
# via authenticateV1 (API key da escrita TRIAGEM_WRITE_API_KEY no Vault, escopo
# write:triagem OU sessao do cockpit); o servidor aplica a classificacao
# DETERMINISTICA. A API key NAO e JWT -> sem este pin o gateway barra o Bearer
# com {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo. Sem o pin, deploy sem
# --no-verify-jwt reabre o bug e nenhum veredito e salvo.
[functions.v1-triagem-veredito]
verify_jwt = false

# Write path da extracao de itens (modo lion): a Lia posta a(s) lista(s) de
# itens extraidas via authenticateV1 (mesma API key de escrita TRIAGEM_WRITE,
# escopo write:triagem). A API key NAO e JWT -> sem este pin o gateway barra o
# Bearer com {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo.
[functions.v1-documento-itens-gravar]
verify_jwt = false

# Diagnostico read-only de recall do Effecti (modo lion): o orquestrador de
# triagem chama para saber se a extracao do aviso esta completa (faltantes do
# piso) e decidir re-despachar o Extrator ANTES do veredito, via authenticateV1
# (API key read-only LIA_SERVICE_API_KEY, escopo read-only:busca-semantica). A
# API key NAO e JWT -> sem este pin o gateway barra o Bearer com
# {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo.
[functions.v1-triagem-recall-check]
verify_jwt = false

# Consulta de politica de participacao (modo lion): a Lia identifica os produtos
# nos itens do edital e consulta a politica deterministica de cada um via
# authenticateV1 (API key read-only LIA_SERVICE_API_KEY, escopo
# read-only:busca-semantica). A API key NAO e JWT -> sem este pin o gateway barra
# o Bearer com {UNAUTHORIZED_INVALID_JWT_FORMAT} antes do codigo.
[functions.v1-politica-participacao]
verify_jwt = false

# Descarte fisico governado da triagem: o pg_cron (triagem-descarte) dispara
# este Edge via pg_net com X-Cron-Secret PURO (sem Authorization). Com verify_jwt
# no default (ON) o gateway barraria com 401 antes do codigo -> descarte agendado
# morto em silencio. A funcao se autentica sozinha (assertInternalAuth:
# X-Cron-Secret do Vault OU service_role bearer). Sem este pin, deploy sem
# --no-verify-jwt reabre o bug.
[functions.triagem-descarte-cron]
verify_jwt = false

# Backfill one-shot da indexacao do catalogo de produtos no escopo
# produto-cotacao: chamada manual com service_role Bearer (sem JWT de usuario).
# A funcao se autentica sozinha (service_role Bearer OU X-Cron-Secret).
[functions.produtos-indexar]
verify_jwt = false

# Retry de favoritar no Effecti da triagem: o pg_cron (triagem-favoritar-retry)
# dispara este Edge via pg_net com X-Cron-Secret PURO (sem Authorization). Com
# verify_jwt no default (ON) o gateway barraria com 401 antes do codigo -> retry
# agendado morto em silencio. A funcao se autentica sozinha (assertInternalAuth:
# X-Cron-Secret do Vault OU service_role bearer). Sem este pin, deploy sem
# --no-verify-jwt reabre o bug.
[functions.triagem-favoritar-retry]
verify_jwt = false

# Coleta (descoberta) do Gmail migrada do GitHub Actions para Edge nativo: o
# pg_cron (coleta-gmail) e a Edge gmail-disparar acionam este Edge via pg_net
# com X-Cron-Secret PURO (sem Authorization). Com verify_jwt no default (ON) o
# gateway barraria com 401 {UNAUTHORIZED_NO_AUTH_HEADER} antes do codigo -> ciclo
# agendado morto em silencio. A funcao se autentica sozinha (matchesCronSecret:
# X-Cron-Secret do Vault). Sem este pin, deploy sem --no-verify-jwt reabre o bug.
[functions.gmail-coletar]
verify_jwt = false

# Mesma razao do gmail-coletar: a descoberta do Drive migrou para Edge nativo e e
# acionada por pg_cron (coleta-drive) / Edge drive-disparar via pg_net com
# X-Cron-Secret PURO (sem Authorization). verify_jwt no default barraria no
# gateway com 401 antes do codigo. A funcao se autentica sozinha
# (matchesCronSecret). Sem este pin, deploy sem --no-verify-jwt reabre o bug.
[functions.drive-coletar]
verify_jwt = false

# O servico de poll do PC consome a fila de comandos (comando-local-fila) com
# X-Cron-Secret PURO (sem Authorization) — o PC nao tem service_role nem sessao
# de usuario. verify_jwt no default (ON) faria o gateway barrar com 401 antes do
# codigo. A funcao se autentica sozinha (matchesCronSecret). Sem este pin, deploy
# sem --no-verify-jwt reabre o bug. (comando-local-enfileirar fica no default ON:
# e a ponta do cockpit, exige sessao de usuario.)
[functions.comando-local-fila]
verify_jwt = false

# A ponta do PC (comando-poll) streama as linhas de stdout/stderr da coleta local
# (Nomus/Tika) para esta funcao com X-Cron-Secret PURO (sem Authorization). Como
# as demais funcoes de fila/cron, verify_jwt no default (ON) faria o gateway barrar
# com 401 antes do codigo (matchesCronSecret autentica internamente). A leitura do
# console (coleta-log) fica no default ON: e a ponta do cockpit, exige sessao.
[functions.coleta-log-ingestar]
verify_jwt = false
