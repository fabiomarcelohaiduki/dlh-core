// =====================================================================
// _shared/block-adapters/nomus-block-adapter.ts
//
// DESATIVADO (D1 — Effecti-only). O seam de bloco (block-source.ts) e
// Effecti-only: o Nomus permanece na sua maquina de bloco propria
// (nomus-pipeline.ts, INALTERADO) e NAO e adotado pelo seam. Este arquivo foi
// esvaziado para nao reintroduzir a fronteira Nomus -> seam que viola D1.
//
// E um arquivo untracked (nunca commitado); pode ser removido do working tree
// com seguranca. Mantido como modulo vazio apenas para o type-check
// (`deno check **/*.ts`) permanecer verde ate a remocao.
// =====================================================================

export {};
